"""SkyyProfiles 0.1.2 - build script (javassist via jpype). SkyBlock-style profiles for SkyWynn = the class selector.
Run:   python build_skyyprofiles_0.1.2.py          -> SkyyProfiles/SkyyProfiles-0.1.2.jar
       (never --deploy: tools/deploy_set.py is the only deploy path; HANDOFF section 3)
GENERATED by tools/profiles_0_1_2_patch.py from build_skyyprofiles_0.1.1.py - edit the patch, never this file (0.1.1 was a copy + edit
of 0.1). Everything below the 0.1.2 and 0.1.1 blocks is the 0.1 design, unchanged unless a line says 0.1.1 or 0.1.2.

0.1.2 (2026-09-25; research/Classes-Berserker-Priest-Spec.md section 4 - Skyy: Berserker back, new Priest class, a class kit per class;
  built with SkyyClasses 0.1.6 + SkyySkills 0.4.4 - DEPLOY THE THREE TOGETHER, spec section 5):
  ROSTER (fallback table, used when SkyyClasses is absent; class:list decides what is selectable when it is present): Archer, Warrior,
    Mage, BERSERKER (Fury - axes, battleaxes, maces, clubs) and PRIEST (Divinity - wands, spellbooks) selectable; Assassin and Shaman
    'coming later' (Shaman: a custom weapon later, Slowness Totem icon - the wand is a Priest weapon now). Names, skills, colors, weapon
    texts and descriptions = SkyyClasses 0.1.6 (spec 2.1). ProfRoster entries grow to 8 fields: role at index 7, one line per class
    (Priest = 'AoE healer / support'; an extra class published only by SkyyClasses gets 'A class from SkyyClasses'). With SkyyClasses
    0.1.5 (no Berserker / Priest in class:list) both cards show greyed 'coming later' and cannot be picked.
  CREATE PROFILE PAGE: up to MAX_CARDS = 8 class cards, drawn AND handled (0.1.1 drew 6 and handled pfcls0..5 - with a 7-entry roster
    Priest was invisible); a role line per card in the class color. ONE root for both views of ProfilePage: 1000 x 900 (was 780 x 600;
    fits 1080), padding 16 / 10 = a 968 x 880 content box. Create view: bar 2, title 36, name row 36, sub 34, 7 x (gap 4 + card 90),
    gap 8, make row 40, info 24 = 838 px (a rare 8th card - a class this build does not know - switches to gap 2 + card 84 = 868 px).
    List view: the same content scaled ~1.2x (cards 76 high, fonts +2, buttons 14 pt). The numbers are build constants (CL / LL) that
    the build checks against the content box. Element ids unchanged. While SkyyClasses publishes class:fn:kitnew the sub line says
    the class kit (its basic weapon) arrives right after creating the profile.
  CLASS KIT HAND-OFF (spec 4.3): ProfKit.kitNew(uuid, key, class) calls the SkyyClasses 0.1.6 bridge function class:fn:kitnew
    (apply(Object[] { UUID, String pkey, String class }) -> Boolean) right after a profile is created, OUTSIDE every SkyyProfiles
    monitor (after ProfStore.create and publish returned): createFirst with key uuid (profile 1); createAndSwitch with
    keyFor(uuid, id) BEFORE switchTo (a failed switch still leaves the kit pending for the day the player switches to that profile).
    SkyyClasses owns the rest (kit state per profile, delivery on the ACTIVE key after profile:busy clears and 31 s after the epoch
    change, storage first, claims via /class kit). Missing function = SkyyClasses absent or older = nothing; an answer other than TRUE
    or an exception logs ONE warning per JVM and, for EVERY miss, a KIT-MISSED line in switches.log (uuid, profile key, class, why:
    the admin's list for /classadmin kit). /profileadmin setclass never calls it (a fix, not a pick; its reply points admins at
    /classadmin kit).
  ONE CREATE PER SELECTION (review fix): the pfmake handler takes pickClass BEFORE any create work and puts it back only where the
    create form shows again with nothing created; a first-profile form whose player already has a profile goes to the list instead
    of creating a second one. PageManager already drops clicks while an update / close is unacknowledged (verified: rebuild and
    setPage(None) both add a required ack, setPage(None) also nulls customPage); this covers the rest (an exception between the create
    and the close, a close that silently failed, a create by command while the page was open) so no stale click makes an extra profile
    and with it an extra class kit.
  Texts: /profileadmin setclass class help, manifest description. No new config rows, Settings switches or files; players files and
  every bridge key unchanged (profile:class:<uuid> may now also be Berserker or Priest, as tools/PROFILES-CONTRACT.md lists).

0.1.1 (2026-09-25; research/Server-Setup-Spec.md 4.16 + 7, research/Settings-Spec.md 3.9, tools/CONFIG-CONTRACT.md):
  DEFAULT CAP 6 (Skyy locked 2026-09-24, was 4): DEF_MAX_PROFILES = 6; the 1-6 clamp is unchanged (no way above 6 exists - Skyy's open
    decision, not invented here). A config.properties that is still BYTE FOR BYTE the file 0.1 wrote by itself (nobody ever edited it)
    is replaced by the 0.1.1 default file once at start (ProfCfg.upgradeDefault, before the kit starts; one server log line). Any file
    an owner touched keeps its maxProfiles value - they change it in game.
  ADMIN CONFIG (tools/skyycfg.py; SkyWynn Menu > Server Setup > Profiles, /modconfig profiles): config:def:SkyyProfiles +
    config:fn:SkyyProfiles + config:epoch:SkyyProfiles, published at the END of setup() after the config load. Node skyyprofiles.admin.
    File Skyy_SkyyProfiles/config.properties (names and keys unchanged). Every row is field:-bound to the ProfCfg volatile it always
    had; RELOAD = ProfCfg.load (hand edits noticed by the kit, and /profileadmin reload, re-run the 0.1 loader with its clamps).
      maxProfiles         int 6 (1-6)          live, danger   lowering never deletes a profile; it only stops creating
      promptEveryLogin    bool true            live
      openDelayMillis     int 2000 ms          live, adv      (250-60000; OPEN_DELAY_MS stores ms, so no scale)
      combatSeconds       int 10 s (0-600)     live           field ProfCfg.COMBAT_MS*1000 (the field stores milliseconds)
      islandOnSwitch      bool true            live
      perProfileBackpack  bool true            live, danger
      newProfileBackpack  int 0 (-1..256)      new            (used when a never-played profile is first entered)
      keepItems           items Skyy_Menu      live, danger   KEEP now holds the canonical "a,b" list (0.1: ",a,b,"); keep() adds the
                                                              commas itself. Skyy_Menu is SkyyMenu's own item (not in Assets.zip), so
                                                              the build check gets ITEMS = Assets.zip items + Skyy_Menu.
    Commands: /profileadmin reload goes through the kit's reload op (hand edits logged via=file, the loader runs after the kit's pending
    writes); /profileadmin (no args) prints the running values without re-reading the file (0.1 re-read it, which could undo a value set
    in the menu a moment earlier); NEW /profileadmin config (every setting + value) and /profileadmin set <setting> <value|default>
    (CfgFn.cmdSet: validated, confirm implied, logged via=command, versioned - the same path as the menu). A command argument cannot be
    empty, so "none" clears keepItems (the kit accepts an empty list); "default" for keepItems needs SkyyMenu installed (the kit checks
    Skyy_Menu against the live item map and refuses it otherwise - the reply then explains "none" or a hand edit + reload).
  PLAYER SETTING (SkyyMenu 0.2+ registry): profiles.loginStatus "Profile on login" (tab profiles, default ON) registered in setup();
    the only gate is the "Playing profile X (Class)" chat line in ReadyTask. Nothing on the create / switch / recovery path changed.
    Without SkyyMenu notifyOn answers true = 0.1 behaviour.
  Profiles page footer: when the cap was lowered below a player's count it says they keep their profiles (was "6 of 4 slots used").
  Default behaviour is otherwise identical to 0.1 until an admin changes a value.

Skyy (2026-09-23): "start profiles, and make class selection part of creating the profile like Minecraft so you are locked into your class."
Contract implemented: tools/PROFILES-CONTRACT.md (v1). One profile = one class (chosen at creation, never changeable) + its own island,
coins, bank, bags, skills, collections, accessories (the other mods key their files by pkey) + its own VANILLA INVENTORY (this mod).

PROFILES: ids "1".."N" per player (config maxProfiles, default 6 since 0.1.1, 1-6). Each has a name, a class (locked at creation), created, lastPlayed.
  Names: SkyBlock-style fruit names (FRUITS below), picked at random among the unused ones; "Other name" rerolls. No typed names in 0.1:
  a TextField exists in vanilla .ui templates (NameRespawnPointPage) but inline TextField syntax is unproven on Skyy's client (a parse
  error disconnects the client on the join path) - fruit names are the safe choice (task rule).
  Storage key (contract): profile "1" = uuid.toString() (= every existing data file, no migration), profile N = uuid + "-p" + N.
CREATE PROFILE page (Minecraft create-world style): class cards from the SkyyClasses bridge (class:list = the ENABLED classes, "Name:Skill")
  merged with a built-in fallback roster (0.1.2: Archer, Warrior, Mage, Berserker, Priest selectable; Assassin, Shaman "coming later");
  pick one, "Create profile".
  First join with no profile: the page opens ~openDelayMillis (2 s) after the first PlayerReadyEvent of the session (SkyyHud/SkyyClasses
  OpenTask pattern: world-thread hop + WorldMap channel gate) and creates profile "1" (legacy key = everything the player already has).
  Cross-mod timeline pass: the open waits until the player has stayed in one world for 2 polls (re-checked on that world's thread),
  waits up to 8 s while they stand in a skyy-island-* world (SkyyIslands routes island logins to the hub 1.5 s after the first
  PlayerReadyEvent; a page opened just before that teleport is lost), never replaces an open custom page (SkyyMenu etc.: it waits;
  a Profiles page the player opened with /profiles: done) and never opens over the respawn screen.
  A class the player already had in SkyyClasses 0.1.x (bridge class:<uuid>, else class:fn:get) is pre-selected. "Later" / Esc closes it;
  it re-opens on every login until profile 1 exists (promptEveryLogin=true) - /profiles or /profiles create opens it any time.
PROFILES page (/profiles, alias /profile): a card per profile (class icon, name, class + combat skill, created, last played, ACTIVE),
  Switch -> Confirm/Cancel, "Create new" while slots are free. Positional forms: /profiles create (alias new), /profiles switch <n|name>,
  /profiles list. Everyone may use them (setPermissionGroups hytale:Adventurer on the root and every subcommand, HANDOFF COMMAND RULES).
SWITCH (world thread, one uninterrupted task, per-player BUSY guard):
  0. refuse while dead (DeathComponent), in combat (DamageDataComponent lastDamageTime / lastCombatAction within combatSeconds=10,
     measured with the world's TimeResource like DamageSystems$RecordLastCombat) or while a custom page other than ours is open.
  1. read + parse the TARGET profile's inventories/<toKey>.json first (a profile never switched away from = brand new = empty);
     a missing/unreadable file for a profile that should have one refuses the switch with nothing changed.
  2. capture EVERY vanilla inventory section - InventoryComponent Hotbar, Storage, Backpack, Armor, Utility, Tool (verified: these six
     components are all Inventory.getSectionById serves, InventoryUtils.clear clears InventoryComponent.EVERYTHING) - each slot's full
     ItemStack: id, quantity, durability, max durability, quality index, overrideDroppedItemAnimation, metadata BsonDocument, plus the
     engine's own ItemStack.CODEC encoding (restored first; the explicit fields are the fallback + human-readable). Written to
     inventories/<fromKey>.json as EXTENDED JSON (lossless BSON types) atomically (tmp + fsync + ATOMIC_MOVE) and read back + counted
     BEFORE anything is cleared.
  3. write the marker switching/<uuid>.properties (from, to, fromKey, toKey, toHadFile, stage=saved) - atomic + fsync.
  4. clear all sections (ItemContainer.clear / removeItemStackFromSlot, verified empty afterwards).
  5. load the target snapshot (backpack SIZE is per profile too when perProfileBackpack=true: vanilla Backpack.resize, as the vanilla
     /inventory backpack command; a new profile starts at newProfileBackpack=0 like a new vanilla player). Each stack goes back to its
     slot with setItemStackForSlot(slot, stack, false) (verified by reading the slot); anything that cannot -> addOrDropItemStack into
     storage-hotbar-backpack (storage first).
  6. players/<uuid>.properties: active=to, epoch+1, from.inv=1 (atomic); profile:fn:key returns the new key from this instant.
  7. the marker is KEPT 30 s (ClearLater), not deleted: the engine saves a player's inventory at most every 10 s (PlayerSavingSystems
     TickingSystem), so a crash right after a switch would reload the PRE-switch inventory under the new profile (one profile duplicated,
     the other lost). With the marker the next join rolls forward to the new profile's snapshot instead. A graceful stop (plugin
     shutdown: the engine saves every online player) deletes pending markers at once; a rejoin inside the same JVM never recovers a
     pending marker (PENDING). 8. republish the bridge, close our page,
     dispatch the player's own "/island" (CommandManager.get().handleCommand(playerRef, "island") - SkyyIslands 0.4.4 sends them to the
     ACTIVE profile's island, created on first use); without SkyyIslands -> the default world spawn (SkyyIslands HubCmd fallback calls).
  profile:busy:<uuid> (Boolean) is present for the whole transaction (steps 1-8 run in ONE world-thread task, so only off-thread readers
  can ever see it; it matters for crash recovery, below).
  Failure inside 4-6 -> immediate rollback (clear + reload the step-2 snapshot, marker removed). If even that fails the marker becomes
  stage=failed (never auto-applied; the snapshots stay on disk for an admin) and the player is told. While a stage=failed marker exists
  every switch / create-and-switch is refused (it would overwrite the snapshots the admin needs) until an admin deletes the marker.
  keepItems (config, default Skyy_Menu = the SkyyMenu item): never saved, never cleared - it stays on every profile (SkyyMenu gives its
  item once per PLAYER, so a new profile would otherwise have no menu item).
JOIN (PlayerConnectEvent - engine join thread, fired BEFORE the player is added to any world; registerGlobal like BetterMap/Essentials):
  re-read players/<uuid>.properties in place (the cached copy is replaced, never dropped), note the username, repair a missing/invalid
  active id (-> lowest profile), plan crash recovery, publish every per-player bridge key. So the very first thing any other mod can see
  of a joining player already carries the right key and epoch. Fallback: the first PlayerReadyEvent of the session does the same when
  the connect handler did not run. Chat messages (welcome, recovery result) wait for that first PlayerReadyEvent (NOTICE).
CRASH SAFETY: a leftover marker at join (planned at PlayerConnectEvent, files read there; applied on the player's world thread as soon as
  the entity is in a world - not after PlayerReadyEvent - while profile:busy:<uuid> is set so adopters leave the inventory alone):
  stage=saved + players file active == from -> ROLL BACK: clear, load inventories/<fromKey>.json (the snapshot the marker was written
  after). active == to -> ROLL FORWARD: clear, load the target snapshot (or empty for a new profile). Either way the live inventory becomes
  exactly the snapshot of the ACTIVE profile and the other profile's items stay in their file: no loss, no duplication.
  The load runs in rollbackMode exactly like the live rollback: a slot the clear could not empty that already holds the identical saved
  stack (same id + qty) counts as restored and is never given again. If the clear could not verify every slot empty, the recovery is NOT
  reported as clean: the marker becomes stage=failed with a note (which slots to check for duplicates = server log "could not empty"),
  the player is told and switching pauses until an admin deletes the marker.
  A clean recovery keeps its marker 30 s too (same reason as switch step 7).
  stage=done -> just deleted. stage=failed -> reported (log + player), left for an admin.
  Windows: a file open for reading cannot be replaced or deleted (AccessDeniedException, tested on the game JRE), so every players-file
  read runs under the same lock as the writes, a marker is only read while no switch/clear of that player can run (BUSY), and atomic
  moves / marker deletes retry 5x 20 ms before giving up (the atomic replace on any FileSystemException, not only AccessDeniedException:
  another program holding the file can surface as a plain sharing-violation FileSystemException).
ADMIN (perm skyyprofiles.admin): /profileadmin info <player|uuid>, /profileadmin setclass <player|uuid> <n> <class> (fix mistakes; bumps
  the epoch when it is the active profile), /profileadmin reload (config through the kit + re-read player files), 0.1.1:
  /profileadmin config, /profileadmin set <setting> <value|default>.
BRIDGE (System.getProperties().get("skyy.bridge")), per-player keys published at PlayerConnectEvent and after every change, epoch LAST
  (full semantics for adopters: tools/PROFILES-CONTRACT.md, "Semantics of SkyyProfiles 0.1"):
  profile:fn:key          Function apply(UUID) -> storage key of the ACTIVE profile. Registered in setup(), never removed (valid
                          through other mods' shutdown saves). Cache hit = one ConcurrentHashMap read, no lock, no I/O; first request
                          for a player not cached yet (offline, or before the connect preload) reads the players file synchronously
                          under the store lock. Never throws, never calls another mod. AUTHORITATIVE - the String keys below mirror it.
  profile:key:<uuid>      same String            profile:<uuid>        active id "1", "2", ... (absent = no profile yet)
  profile:class:<uuid>    locked class of the active profile (absent = none)   profile:name:<uuid>  its name
  profile:epoch:<uuid>    Long, +1 on every creation and switch (and admin setclass of the active profile); 0 = no profile yet
  profile:list:<uuid>     extra (not in contract v1): "1:Apple:Archer,2:Banana:Mage"
  profile:busy:<uuid>     extra: Boolean.TRUE while the live vanilla inventory may not belong to the active profile (a switch, or a
                          pending crash recovery at join). Adopters that move items between the inventory and per-profile storage skip.
  Per-player keys are NOT removed on disconnect (they keep describing the player's active profile, which cannot change while offline
  except by /profileadmin setclass, which republishes).
SCHEMA (Skyy_SkyyProfiles/, everything keyed by player UUID; this mod owns the profile list):
  config.properties        maxProfiles, openDelayMillis, promptEveryLogin, combatSeconds, islandOnSwitch, perProfileBackpack,
                           newProfileBackpack, keepItems (0.1.1: also written line by line by the config kit)
  config-changes.log, config-history/   0.1.1: the kit's change log and file versions (tools/CONFIG-CONTRACT.md)
  players/<uuid>.properties  username, active, epoch, switches, prompted, p.<id>.name, p.<id>.class, p.<id>.created,
                           p.<id>.lastPlayed (epoch millis), p.<id>.inv=1 (only while that profile is NOT active: its items are on disk)
  inventories/<key>.json   {format:1, mod, uuid, profile, key, savedAt, backpackCapacity, count, sections:[{name, capacity,
                           slots:[{slot, id, qty, durability, maxDurability, quality, overrideAnim, meta, stack}]}]}  (EXTENDED JSON)
  switching/<uuid>.properties  marker {from, to, fromKey, toKey, toHadFile, stage=saved|done|failed, at, note (failed recoveries)}
  switches.log             one line per CREATE / SWITCH / ROLLBACK / RECOVER / RECOVER-INCOMPLETE / KIT-MISSED (0.1.2: SkyyClasses did
                           not record the class kit of a new profile) (admin audit trail)
"""
import sys, os, re, zipfile
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "tools"))
import skyybuild as B
import skyycfg as CFG

VERSION = "0.1.2"
HERE = os.path.dirname(os.path.abspath(__file__))


def need(ok, msg):
    """Build-time guard: an explicit raise, never assert (python -O / PYTHONOPTIMIZE strips asserts; the kit's CfgError does the same)."""
    if not ok:
        raise SystemExit("SkyyProfiles build: " + str(msg))

# =====================================================================================================================
# DATA (edit here, rebuild). UI text: no , : ; { } " ' _ \ (the inline UI parser is picky; the build asserts it).
# Fallback roster = SkyyClasses 0.1.6 (used when SkyyClasses is not loaded; class:list decides which are selectable when it is).
# 0.1.2 (research/Classes-Berserker-Priest-Spec.md 2.1 / 4.1): order = card order; Berserker + Priest added, Shaman's icon is the Slowness
# Totem (the wand is a Priest weapon now); role = the one line under the combat skill on the Create Profile card.
# =====================================================================================================================
CLASSES = [
    {"name": "Archer", "skill": "Archery", "color": "#8fd67a", "enabled": True, "role": "Ranged damage",
     "desc": "Fights from range. Charge a shortbow or load a crossbow and strike before the enemy gets close. Arrows are Archer ammo.",
     "icons": ["Weapon_Shortbow_Iron", "Weapon_Crossbow_Iron", "Weapon_Arrow_Crude"], "weapon_text": "Shortbows / Crossbows"},
    {"name": "Warrior", "skill": "Swordsmanship", "color": "#e0b060", "enabled": True, "role": "Melee fighter",
     "desc": "Front line blade fighter. Swords and longswords up close and spears for reach.",
     "icons": ["Weapon_Sword_Iron", "Weapon_Longsword_Iron", "Weapon_Spear_Iron"], "weapon_text": "Swords / Longswords / Spears"},
    {"name": "Mage", "skill": "Sorcery", "color": "#7fb0e0", "enabled": True, "role": "Magic damage",
     "desc": "Spellcaster. Staves strike up close and cast magic at range.",
     "icons": ["Weapon_Staff_Iron", "Weapon_Staff_Wizard", "Weapon_Staff_Crystal_Ice"], "weapon_text": "Staves"},
    {"name": "Berserker", "skill": "Fury", "color": "#d9443f", "enabled": True, "role": "Heavy melee damage",
     "desc": "Heavy melee fighter. Axes and battleaxes cleave - maces and clubs crush. Hatchets stay gathering tools for everyone.",
     "icons": ["Weapon_Battleaxe_Iron", "Weapon_Axe_Iron", "Weapon_Mace_Iron"], "weapon_text": "Axes / Battleaxes / Maces / Clubs"},
    {"name": "Priest", "skill": "Divinity", "color": "#f2e6a0", "enabled": True, "role": "AoE healer / support",
     "desc": "AoE healer and support. Your wand and spellbook hits on monsters heal party members near you. Spells come later.",
     "icons": ["Weapon_Wand_Wood", "Weapon_Spellbook_Grimoire_Brown", "Weapon_Spellbook_Frost"], "weapon_text": "Wands / Spellbooks"},
    {"name": "Assassin", "skill": "Assassination", "color": "#b58cff", "enabled": False, "role": "Fast burst damage",
     "desc": "Coming later. Fast and deadly with twin daggers and kunai throwing knives.",
     "icons": ["Weapon_Daggers_Iron", "Weapon_Kunai"], "weapon_text": "Daggers / Kunai"},
    {"name": "Shaman", "skill": "Shaman skill", "color": "#ff7a5c", "enabled": False, "role": "Designed later",
     "desc": "Coming later. The fifth Wynncraft class - it gets its own custom weapon.",
     "icons": ["Weapon_Deployable_Slowness_Totem"], "weapon_text": "Custom weapon later"},
]
FRUITS = ["Apple", "Banana", "Blueberry", "Coconut", "Cucumber", "Grapes", "Kiwi", "Lemon", "Lime", "Mango", "Orange",
          "Papaya", "Pear", "Pineapple", "Pomegranate", "Raspberry", "Strawberry", "Tomato", "Watermelon", "Zucchini"]
NEW_ICON = "Ingredient_Voidheart"      # "Create new" card + profiles without a class
EXTRA_ICON = "Weapon_Sword_Iron"       # a class published by SkyyClasses that this build does not know
EXTRA_ROLE = "A class from SkyyClasses"  # 0.1.2: its role line (spec 4.1)
EXTRA_DESC = "Open /class after creating the profile to see its weapons."   # 0.1.2 (was the role text a second time)
DEF_MAX_PROFILES = 6                   # 0.1.1: Skyy locked 6 (2026-09-24), was 4. The 1-6 clamp stays (no way above 6 yet)
DEF_OPEN_DELAY_MS = 2000
DEF_COMBAT_S = 10
DEF_KEEP = "Skyy_Menu"
MAX_ID = 64                            # highest profile id ever scanned in a player file
PAGE_W, PAGE_H = 1000, 900             # 0.1.2: ONE root for both views (was 780 x 600); fits 1080 (spec 4.2)
PAD_H, PAD_V = 16, 10                  # root padding of both views: content box (PAGE_W - 2*PAD_H) x (PAGE_H - 2*PAD_V) = 968 x 880
MAX_CARDS = 8                          # 0.1.2: class cards drawn on the Create Profile page AND pfcls<i> payloads handled (0.1.1: 6)
# Create Profile view (spec 4.2), px. Heights: BAR TITLE NAME SUB, per card GAP + CARD, then AFTER MAKE INFO. Card text column: TPAD T1
# (title, 16 pt) T2 (combat skill + weapons, 12 pt bold) T3 (role, 12 pt, class color) T4 (description, 11 pt wrapped; 28 = the spec's
# 26 + the 2 px the 90 px card had spare, room for the second line). *8 = the rare 8-card page (a class this build does not know).
# Widths: LPAD + 3 x ICO (ItemIcon ICON at ICOL) + TEXT + ACT (button BTNW x BTNH); name row NAMEL + NAMEB; make row MAKEL + MAKEB + 10 + BACKB.
# Buttons 150 x 34 at 14 pt: spec 4.2 fixes only the 170 action column (160 x 40 at 15 pt in spec 2.6 is the SkyyClasses /class page);
# BTNH is also the make row's button height, and 40 + its 3 px top padding would not fit the 40 px make row.
CL = {"BAR": 2, "TITLE": 36, "NAME": 36, "SUB": 34, "GAP": 4, "CARD": 90, "AFTER": 8, "MAKE": 40, "INFO": 24,
      "TPAD": 2, "T1": 24, "T2": 18, "T3": 18, "T4": 28, "GAP8": 2, "CARD8": 84, "TPAD8": 0, "T18": 22, "T48": 26,
      "LPAD": 8, "ICO": 52, "ICON": 44, "ICOL": 4, "TEXT": 600, "ACT": 170, "BTNW": 150, "BTNH": 34,
      "NAMEL": 760, "NAMEB": 170, "NAMEBH": 32, "MAKEL": 600, "MAKEB": 170, "BACKB": 150}
CL["ICOS"] = 3 * CL["ICO"]
# Profiles list view: 0.1.1 scaled ~1.2x (cards 76 high, fonts +2). Heights: BAR TITLE SUB, per profile GAP + CARD, the free-slot card
# GAP + NEW, then AFTER INFO and CONF (confirm row) or FOOT. Card text column: TPAD T1 (name, 17 pt) T2 (class, 14 pt) T3 (dates, 13 pt).
LL = {"BAR": 3, "TITLE": 36, "SUB": 24, "GAP": 8, "CARD": 76, "NEW": 68, "AFTER": 10, "INFO": 26, "CONF": 40, "FOOT": 36,
      "TPAD": 6, "T1": 26, "T2": 22, "T3": 20,
      "LPAD": 12, "ICO": 72, "ICON": 52, "ICOL": 10, "NEWICON": 44, "NEWICOL": 14, "TEXT": 620, "ACT": 200, "BTNW": 180, "BTNH": 36,
      "CONFL": 560, "CONFB": 150}
LL["ICOT"] = (LL["CARD"] - LL["ICON"]) // 2
LL["ACTT"] = (LL["CARD"] - LL["BTNH"]) // 2
LL["NEWICOT"] = (LL["NEW"] - LL["NEWICON"]) // 2
LL["NEWACTT"] = (LL["NEW"] - LL["BTNH"]) // 2
_CW, _CH = PAGE_W - 2 * PAD_H, PAGE_H - 2 * PAD_V
need(PAGE_H <= 1080 - 60, "page taller than a 1080 screen allows")
_c_head = CL["BAR"] + CL["TITLE"] + CL["NAME"] + CL["SUB"]
_c_tail = CL["AFTER"] + CL["MAKE"] + CL["INFO"]
_c7 = _c_head + 7 * (CL["GAP"] + CL["CARD"]) + _c_tail
_c8 = _c_head + MAX_CARDS * (CL["GAP8"] + CL["CARD8"]) + _c_tail
need(len(CLASSES) <= 7, "the 90 px card layout is sized for at most 7 fallback classes (8 = tight variant for one extra)")
need(MAX_CARDS == 8, "the tight card variant is sized for exactly 8 cards")
need(_c7 <= _CH, "create view with 7 cards is %d px, content box %d" % (_c7, _CH))
need(_c8 <= _CH, "create view with %d cards is %d px, content box %d" % (MAX_CARDS, _c8, _CH))
need(CL["TPAD"] + CL["T1"] + CL["T2"] + CL["T3"] + CL["T4"] <= CL["CARD"], "card text column taller than the card")
need(CL["TPAD8"] + CL["T18"] + CL["T2"] + CL["T3"] + CL["T48"] <= CL["CARD8"], "tight card text column taller than the card")
need(CL["ICON"] <= CL["CARD8"] and CL["BTNH"] <= CL["CARD8"] and CL["NAMEBH"] + 2 <= CL["NAME"] and CL["BTNH"] + 3 <= CL["MAKE"], "create view buttons / icons do not fit their rows")
need(CL["LPAD"] + CL["ICOS"] + CL["TEXT"] + CL["ACT"] <= _CW, "create card wider than the content box")
need(CL["BTNW"] <= CL["ACT"] and CL["ICOL"] + CL["ICON"] <= CL["ICO"], "create card action / icon column too narrow")
need(CL["NAMEL"] + CL["NAMEB"] <= _CW and CL["MAKEL"] + CL["MAKEB"] + 10 + CL["BACKB"] <= _CW, "create view name / make row wider than the content box")
_l_head = LL["BAR"] + LL["TITLE"] + LL["SUB"]
_l_tail = LL["AFTER"] + LL["INFO"] + max(LL["CONF"], LL["FOOT"])
_l6 = _l_head + 6 * (LL["GAP"] + LL["CARD"]) + _l_tail
_l5 = _l_head + 5 * (LL["GAP"] + LL["CARD"]) + LL["GAP"] + LL["NEW"] + _l_tail
need(max(_l6, _l5) <= _CH, "list view is %d px, content box %d" % (max(_l6, _l5), _CH))
need(LL["TPAD"] + LL["T1"] + LL["T2"] + LL["T3"] <= LL["CARD"], "list card text column taller than the card")
need(LL["LPAD"] + LL["ICO"] + LL["TEXT"] + LL["ACT"] <= _CW, "list card wider than the content box")
need(LL["BTNW"] <= LL["ACT"] and LL["ICOL"] + LL["ICON"] <= LL["ICO"] and LL["NEWICOL"] + LL["NEWICON"] <= LL["ICO"], "list card action / icon column too narrow")
need(LL["CONFL"] + 2 * LL["CONFB"] + 10 <= _CW and 34 + 3 <= LL["CONF"], "list view confirm row does not fit")
print("page %dx%d: create view %d px (7 cards) / %d px (%d cards), list view %d px (6 profiles), content box %dx%d"
      % (PAGE_W, PAGE_H, _c7, _c8, MAX_CARDS, max(_l6, _l5), _CW, _CH))

BAD_UI = set(',:;{}"\'_\\')
for _c in CLASSES:
    for _k in ("name", "skill", "desc", "weapon_text", "role"):
        _bad = BAD_UI & set(_c[_k])
        need(not _bad, "UI text %s.%s contains %r" % (_c["name"], _k, "".join(sorted(_bad))))
    need(re.match(r"^#[0-9a-fA-F]{6}$", _c["color"]), "class color %s" % _c["color"])
    need(1 <= len(_c["icons"]) <= 3, "1-3 icons per class")
    need(0 < len(_c["role"]) <= 40, "role of %s: one short line (1-40 characters)" % _c["name"])
    need(len(_c["desc"]) <= 130, "description of %s: at most 130 characters (two lines on the card)" % _c["name"])
need([c["name"] for c in CLASSES] == ["Archer", "Warrior", "Mage", "Berserker", "Priest", "Assassin", "Shaman"], "fallback roster order (spec 4.1)")
need([c["name"] for c in CLASSES if c["enabled"]] == ["Archer", "Warrior", "Mage", "Berserker", "Priest"], "launch roster (Skyy 2026-09-25)")
need(dict((c["name"], c["role"]) for c in CLASSES)["Priest"] == "AoE healer / support", "Priest role text (spec 4.1)")
for _t in (EXTRA_ROLE, EXTRA_DESC):
    need(not (BAD_UI & set(_t)), "UI text %r" % _t)
# review fix: the extra-class texts get the same length limits as every CLASSES entry (role one line, description two lines)
need(0 < len(EXTRA_ROLE) <= 40, "EXTRA_ROLE: one short line (1-40 characters)")
need(0 < len(EXTRA_DESC) <= 130, "EXTRA_DESC: at most 130 characters (two lines on the card)")
for _f in FRUITS:
    need(re.match(r"^[A-Za-z]+$", _f), "fruit name %s" % _f)
need(len(set(FRUITS)) == len(FRUITS) >= 6, "FRUITS needs at least 6 names, no duplicates")
_ASSETS = zipfile.ZipFile(os.path.join(B.HYTALE, "install", "release", "package", "game", "latest", "Assets.zip"))
_ITEMS = set(os.path.basename(n)[:-5] for n in _ASSETS.namelist() if n.startswith("Server/Item/Items/") and n.endswith(".json"))
for _ic in [i for c in CLASSES for i in c["icons"]] + [NEW_ICON, EXTRA_ICON, DEF_KEEP]:
    need(_ic in _ITEMS or _ic == DEF_KEEP, "icon item %s not in Assets.zip" % _ic)
print("roster fallback:", ", ".join("%s(%s)" % (c["name"], "on" if c["enabled"] else "later") for c in CLASSES), "| fruits:", len(FRUITS))

# =====================================================================================================================
# JVM + engine classes
# =====================================================================================================================
J = B.start()
pool, CtField, CtNewMethod, CtNewConstructor = J["pool"], J["CtField"], J["CtNewMethod"], J["CtNewConstructor"]
OUT = B.class_out(HERE)
PKG = "com.skyy.profiles"
T = {
    "PKG": PKG,
    "JP":    "com.hypixel.hytale.server.core.plugin.JavaPlugin",
    "JPI":   "com.hypixel.hytale.server.core.plugin.JavaPluginInit",
    "PR":    "com.hypixel.hytale.server.core.universe.PlayerRef",
    "REF":   "com.hypixel.hytale.component.Ref",
    "ST":    "com.hypixel.hytale.component.Store",
    "CA":    "com.hypixel.hytale.component.ComponentAccessor",
    "CT":    "com.hypixel.hytale.component.ComponentType",
    "UNI":   "com.hypixel.hytale.server.core.universe.Universe",
    "WLD":   "com.hypixel.hytale.server.core.universe.world.World",
    "APC":   "com.hypixel.hytale.server.core.command.system.basecommands.AbstractPlayerCommand",
    "CTX":   "com.hypixel.hytale.server.core.command.system.CommandContext",
    "CMGR":  "com.hypixel.hytale.server.core.command.system.CommandManager",
    "MSG":   "com.hypixel.hytale.server.core.Message",
    "HSV":   "com.hypixel.hytale.server.core.HytaleServer",
    "ATY":   "com.hypixel.hytale.server.core.command.system.arguments.types.ArgTypes",
    "RA":    "com.hypixel.hytale.server.core.command.system.arguments.system.RequiredArg",
    "LOG":   "com.hypixel.hytale.logger.HytaleLogger",
    "PLA":   "com.hypixel.hytale.server.core.entity.entities.Player",
    "PAGE":  "com.hypixel.hytale.server.core.entity.entities.player.pages.CustomUIPage",
    "PGE":   "com.hypixel.hytale.protocol.packets.interface_.Page",
    "LIFE":  "com.hypixel.hytale.protocol.packets.interface_.CustomPageLifetime",
    "UCB":   "com.hypixel.hytale.server.core.ui.builder.UICommandBuilder",
    "UEB":   "com.hypixel.hytale.server.core.ui.builder.UIEventBuilder",
    "EVD":   "com.hypixel.hytale.server.core.ui.builder.EventData",
    "BT":    "com.hypixel.hytale.protocol.packets.interface_.CustomUIEventBindingType",
    "PRE":   "com.hypixel.hytale.server.core.event.events.player.PlayerReadyEvent",
    "PDE":   "com.hypixel.hytale.server.core.event.events.player.PlayerDisconnectEvent",
    "PCE":   "com.hypixel.hytale.server.core.event.events.player.PlayerConnectEvent",
    "INVC":  "com.hypixel.hytale.server.core.inventory.InventoryComponent",
    "HOT":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Hotbar",
    "STO":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Storage",
    "BAK":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Backpack",
    "ARM":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Armor",
    "UTI":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Utility",
    "TOO":   "com.hypixel.hytale.server.core.inventory.InventoryComponent$Tool",
    "IC":    "com.hypixel.hytale.server.core.inventory.container.ItemContainer",
    "SIC":   "com.hypixel.hytale.server.core.inventory.container.SimpleItemContainer",
    "IS":    "com.hypixel.hytale.server.core.inventory.ItemStack",
    "CODEC": "com.hypixel.hytale.codec.Codec",
    "TRF":   "com.hypixel.hytale.math.vector.Transform",
    "TP":    "com.hypixel.hytale.server.core.modules.entity.teleport.Teleport",
    "DDC":   "com.hypixel.hytale.server.core.entity.damage.DamageDataComponent",
    "DEATH": "com.hypixel.hytale.server.core.modules.entity.damage.DeathComponent",
    "TR":    "com.hypixel.hytale.server.core.modules.time.TimeResource",
}
def jv(src):
    """Java source with @TOKEN@ placeholders (raw strings: no brace doubling, \\" stays a Java escape)."""
    out = src
    for k, v in T.items():
        out = out.replace("@" + k + "@", v)
    left = re.findall(r"@[A-Z]+@", out)
    need(not left, "unreplaced tokens: %s" % left)
    return out
def jstr(s):
    return '"' + s.replace("\\", "\\\\").replace('"', '\\"') + '"'
def jarr(xs):
    return "new String[] { " + ", ".join(jstr(x) for x in xs) + " }"

AC = "com.hypixel.hytale.server.core.command.system.AbstractCommand"
for c, m in ((T["PLA"], "getPageManager"), (T["PLA"], "getPlayerConnection"), (T["PLA"], "getInventory"), (T["PLA"], "getComponentType"),
             ("com.hypixel.hytale.server.core.entity.entities.player.pages.PageManager", "getCustomPage"),
             ("com.hypixel.hytale.server.core.entity.entities.player.pages.PageManager", "setPage"),
             ("com.hypixel.hytale.server.core.entity.entities.player.pages.PageManager", "openCustomPage"),
             (T["PAGE"], "rebuild"), (T["PAGE"], "onDismiss"), (T["PGE"], "None"), (T["HSV"], "SCHEDULED_EXECUTOR"),
             (T["UNI"], "getPlayers"), (T["UNI"], "getPlayer"), (T["UNI"], "getWorld"), (T["UNI"], "getDefaultWorld"),
             (T["PR"], "hasPermission"), (T["PR"], "getWorldUuid"), (T["PR"], "getReference"), (T["PR"], "getUsername"), (T["PR"], "isValid"),
             (T["MSG"], "color"), (T["MSG"], "raw"), (T["PRE"], "getPlayerRef"), (T["PDE"], "getPlayerRef"), (T["PCE"], "getPlayerRef"),
             (AC, "addSubCommand"), (AC, "addAliases"), (AC, "requirePermission"), (AC, "setPermissionGroups"), (AC, "withRequiredArg"),
             (T["CMGR"], "get"), (T["CMGR"], "resolveCommand"), (T["CMGR"], "handleCommand"),
             (T["INVC"], "getInventory"), (T["HOT"], "getComponentType"), (T["STO"], "getComponentType"), (T["BAK"], "getComponentType"),
             (T["BAK"], "resize"), (T["ARM"], "getComponentType"), (T["UTI"], "getComponentType"), (T["TOO"], "getComponentType"),
             (T["IC"], "getCapacity"), (T["IC"], "getItemStack"), (T["IC"], "setItemStackForSlot"), (T["IC"], "clear"),
             (T["IC"], "removeItemStackFromSlot"), (T["SIC"], "addOrDropItemStack"),
             ("com.hypixel.hytale.server.core.inventory.Inventory", "getCombinedStorageHotbarBackpack"),
             (T["IS"], "CODEC"), (T["IS"], "getItemId"), (T["IS"], "getQuantity"), (T["IS"], "getDurability"), (T["IS"], "getMaxDurability"),
             (T["IS"], "getQualityIndex"), (T["IS"], "getMetadata"), (T["IS"], "getOverrideDroppedItemAnimation"),
             (T["IS"], "setOverrideDroppedItemAnimation"), (T["IS"], "withQuantity"), (T["IS"], "isEmpty"),
             (T["CODEC"], "encode"), (T["CODEC"], "decode"),
             (T["TP"], "createForPlayer"), (T["TP"], "getComponentType"), (T["WLD"], "getWorldConfig"), (T["WLD"], "execute"), (T["WLD"], "getName"),
             (T["DDC"], "getComponentType"), (T["DDC"], "getLastDamageTime"), (T["DDC"], "getLastCombatAction"),
             (T["DEATH"], "getComponentType"), (T["TR"], "getResourceType"), (T["TR"], "getNow"), (T["ST"], "getResource"),
             ("org.bson.BsonDocument", "parse"), ("org.bson.BsonDocument", "toJson"), ("org.bson.json.JsonWriterSettings", "builder"),
             ("org.bson.json.JsonMode", "EXTENDED"), ("org.bson.BsonValue", "asNumber"),
             ("com.hypixel.hytale.server.core.plugin.PluginBase", "shutdown"),
             ("com.hypixel.hytale.event.EventRegistry", "registerGlobal")):
    B.probe(pool, c, m)

cfg  = pool.makeClass(PKG + ".ProfCfg")
ros  = pool.makeClass(PKG + ".ProfRoster")
pkit = pool.makeClass(PKG + ".ProfKit")        # 0.1.2: class kit hand-off to SkyyClasses (class:fn:kitnew)
nam  = pool.makeClass(PKG + ".ProfNames")
sto  = pool.makeClass(PKG + ".ProfStore")
pub  = pool.makeClass(PKG + ".ProfPub")
kfn  = pool.makeClass(PKG + ".KeyFn")
inv  = pool.makeClass(PKG + ".ProfInv")
sw   = pool.makeClass(PKG + ".ProfSwitch")
clr  = pool.makeClass(PKG + ".ClearLater")
page = pool.makeClass(PKG + ".ProfilePage", pool.get(T["PAGE"]))
opn  = pool.makeClass(PKG + ".OpenTask")
rec  = pool.makeClass(PKG + ".RecoverTask")
pj   = pool.makeClass(PKG + ".ProfJoin")
rtk  = pool.makeClass(PKG + ".ReadyTask")
svt  = pool.makeClass(PKG + ".SaveTask")
rdy  = pool.makeClass(PKG + ".ProfReady")
pcon = pool.makeClass(PKG + ".ProfConnect")
quit_ = pool.makeClass(PKG + ".ProfQuit")
pcre = pool.makeClass(PKG + ".ProfCreateCmd", pool.get(T["APC"]))
psw  = pool.makeClass(PKG + ".ProfSwitchCmd", pool.get(T["APC"]))
plst = pool.makeClass(PKG + ".ProfListCmd", pool.get(T["APC"]))
pcmd = pool.makeClass(PKG + ".ProfilesCmd", pool.get(T["APC"]))
ainf = pool.makeClass(PKG + ".AdmInfoCmd", pool.get(T["APC"]))
acls = pool.makeClass(PKG + ".AdmSetClassCmd", pool.get(T["APC"]))
arel = pool.makeClass(PKG + ".AdmReloadCmd", pool.get(T["APC"]))
acfg = pool.makeClass(PKG + ".AdmConfigCmd", pool.get(T["APC"]))      # 0.1.1
aset = pool.makeClass(PKG + ".AdmSetCmd", pool.get(T["APC"]))         # 0.1.1
adm  = pool.makeClass(PKG + ".ProfileAdminCmd", pool.get(T["APC"]))
pl   = pool.makeClass(PKG + ".SkyyProfilesPlugin", pool.get(T["JP"]))

def F(cls, src): cls.addField(CtField.make(jv(src), cls))
def M(cls, src): cls.addMethod(CtNewMethod.make(jv(src), cls))
def C(cls, src): cls.addConstructor(CtNewConstructor.make(jv(src), cls))

# ================= ProfCfg: logger, bridge, config, atomic file writes =================
# the file 0.1 wrote by itself when none existed (its DEFAULT_LINES, "\n" after every line, UTF-8), kept here byte for byte: a
# config.properties still EXACTLY equal to it was never edited by anyone, so 0.1.1 replaces it by its own default file once (the new
# default cap 6, Skyy 2026-09-24). A file with any change - even a blank line - keeps every value (ProfCfg.upgradeDefault).
CFG_LINES_01 = [
    "# SkyyProfiles config - edit, then /profileadmin reload (or restart the server)",
    "# maxProfiles = profile slots per player (1-6). Each profile = its own class, island, coins, skills, bags and vanilla inventory.",
    "maxProfiles=4",
    "# openDelayMillis = how long after joining the Create Profile page opens for a player without a profile",
    "openDelayMillis=2000",
    "# promptEveryLogin = true: open the Create Profile page on every login until the first profile exists. false: only the very first join",
    "promptEveryLogin=true",
    "# combatSeconds = no profile switch within this many seconds after taking or dealing damage",
    "combatSeconds=10",
    "# islandOnSwitch = true: after a switch the player runs /island (the new profile's island; the world spawn if SkyyIslands is missing)",
    "islandOnSwitch=true",
    "# perProfileBackpack = true: the backpack SIZE (vanilla backpack upgrades) is saved per profile too",
    "perProfileBackpack=true",
    "# newProfileBackpack = backpack size of a brand-new profile (vanilla players start at 0; -1 = keep the current size)",
    "newProfileBackpack=0",
    "# keepItems = item ids that are NOT swapped: they stay in the inventory on every profile (comma list). Skyy_Menu = the SkyyMenu item",
    "keepItems=Skyy_Menu",
]
CFG_LINES = [
    "# SkyyProfiles config - change it in game (SkyWynn Menu > Server Setup > Profiles, or /profileadmin set <setting> <value>),",
    "# or edit this file and run /profileadmin reload (or restart the server). Changes are logged in config-changes.log.",
    "# maxProfiles = profile slots per player (1-6). Each profile = its own class, island, coins, skills, bags and vanilla inventory.",
    "#   Lowering it never deletes a profile: players above it keep theirs but cannot create new ones.",
    "maxProfiles=%d" % DEF_MAX_PROFILES,
    "# openDelayMillis = how long after joining the Create Profile page opens for a player without a profile",
    "openDelayMillis=%d" % DEF_OPEN_DELAY_MS,
    "# promptEveryLogin = true: open the Create Profile page on every login until the first profile exists. false: only the very first join",
    "promptEveryLogin=true",
    "# combatSeconds = no profile switch within this many seconds after taking or dealing damage",
    "combatSeconds=%d" % DEF_COMBAT_S,
    "# islandOnSwitch = true: after a switch the player runs /island (the new profile's island; the world spawn if SkyyIslands is missing)",
    "islandOnSwitch=true",
    "# perProfileBackpack = true: the backpack SIZE (vanilla backpack upgrades) is saved per profile too",
    "perProfileBackpack=true",
    "# newProfileBackpack = backpack size of a brand-new profile (vanilla players start at 0; -1 = keep the current size)",
    "newProfileBackpack=0",
    "# keepItems = item ids that are NOT swapped: they stay in the inventory on every profile (comma list). Skyy_Menu = the SkyyMenu item",
    "keepItems=%s" % DEF_KEEP,
]
F(cfg, "public static java.nio.file.Path FILE;")
F(cfg, "public static @LOG@ LOG;")
F(cfg, "public static final int MAX_ID = %d;" % MAX_ID)
F(cfg, "public static volatile int MAX_PROFILES = %d;" % DEF_MAX_PROFILES)
F(cfg, "public static volatile long OPEN_DELAY_MS = %dL;" % DEF_OPEN_DELAY_MS)
F(cfg, "public static volatile boolean PROMPT_EVERY_LOGIN = true;")
F(cfg, "public static volatile long COMBAT_MS = %dL;" % (DEF_COMBAT_S * 1000))
F(cfg, "public static volatile boolean ISLAND_ON_SWITCH = true;")
F(cfg, "public static volatile boolean PER_PROFILE_BACKPACK = true;")
F(cfg, "public static volatile int NEW_BACKPACK = 0;")
# 0.1.1: the canonical comma list the config kit writes ("Skyy_Menu" or "a,b"); 0.1 stored ",Skyy_Menu," - keep() adds the commas now
F(cfg, "public static volatile String KEEP = %s;" % jstr(DEF_KEEP))
F(cfg, "public static final String[] DEFAULT_LINES = %s;" % jarr(CFG_LINES))
F(cfg, "public static final String[] OLD_LINES = %s;" % jarr(CFG_LINES_01))

# ================= the admin config kit (research/Server-Setup-Spec.md 4.16; tools/CONFIG-CONTRACT.md) =================
# every row binds the ProfCfg volatile 0.1 already had (the fields exist above, before emit); file keys = row keys = the 0.1 file keys.
# RELOAD = ProfCfg.load, the 0.1 loader (clamps as before): run by the kit after a hand edit it noticed and after /profileadmin reload.
CFG_TEXT = "\n".join(CFG_LINES) + "\n"
CFG_CATS = [("profiles", "Profiles"), ("switching", "Switching"), ("inventory", "Inventory")]
CFG_ROWS = [  # (key, label, cat, type, default, min, max, opts, unit, flags, help, binding)
    ("maxProfiles", "Profile slots per player", "profiles", "int", str(DEF_MAX_PROFILES), "1", "6", "step=1", "", "live,danger",
     "Lowering it never deletes a profile: players above it keep theirs but cannot create more.",
     "field:ProfCfg.MAX_PROFILES@config.properties:maxProfiles"),
    ("promptEveryLogin", "Create page at every login", "profiles", "bool", "true", "", "", "", "", "live",
     "On: the Create Profile page opens at each login until a first profile exists. Off: first join only.",
     "field:ProfCfg.PROMPT_EVERY_LOGIN@config.properties:promptEveryLogin"),
    ("openDelayMillis", "Create page delay after joining", "profiles", "int", str(DEF_OPEN_DELAY_MS), "250", "60000", "step=250", "ms",
     "live,adv", "How long after joining the Create Profile page opens for a player without a profile.",
     "field:ProfCfg.OPEN_DELAY_MS@config.properties:openDelayMillis"),
    ("combatSeconds", "No switch after combat", "switching", "int", str(DEF_COMBAT_S), "0", "600", "step=1", "s", "live",
     "Seconds after taking or dealing damage before a profile switch is allowed. 0 = no wait.",
     "field:ProfCfg.COMBAT_MS*1000@config.properties:combatSeconds"),
    ("islandOnSwitch", "Go to the island after a switch", "switching", "bool", "true", "", "", "", "", "live",
     "On: after a switch the player runs /island (that profile's island; world spawn without SkyyIslands).",
     "field:ProfCfg.ISLAND_ON_SWITCH@config.properties:islandOnSwitch"),
    ("perProfileBackpack", "Backpack size per profile", "inventory", "bool", "true", "", "", "", "", "live,danger",
     "On: the backpack size (vanilla backpack upgrades) is saved and restored per profile.",
     "field:ProfCfg.PER_PROFILE_BACKPACK@config.properties:perProfileBackpack"),
    ("newProfileBackpack", "Backpack slots of a new profile", "inventory", "int", "0", "-1", "256", "", "", "new",
     "Backpack slots a brand-new profile starts with (vanilla players start at 0). -1 = keep current size.",
     "field:ProfCfg.NEW_BACKPACK@config.properties:newProfileBackpack"),
    ("keepItems", "Items kept on every profile", "inventory", "items", DEF_KEEP, "", "", "", "", "live,danger",
     "Never swapped on a switch: they stay in the inventory on every profile. Skyy_Menu = SkyyMenu's item.",
     "field:ProfCfg.KEEP@config.properties:keepItems"),
]
need(CFG_TEXT.count("\nmaxProfiles=%d\n" % DEF_MAX_PROFILES) == 1 and ("keepItems=%s\n" % DEF_KEEP) in CFG_TEXT,
     "CFG_TEXT sanity check failed: need exactly one maxProfiles=%d line and a keepItems=%s line" % (DEF_MAX_PROFILES, DEF_KEEP))
# Skyy_Menu is SkyyMenu's own item (its jar's asset pack), not in Assets.zip: the build check of the keepItems default must know it. In game
# the kit checks ids against the live item map, where Skyy_Menu exists whenever SkyyMenu is installed.
kit = CFG.emit(pool, PKG, MOD="SkyyProfiles", TITLE="Profiles", VERSION=VERSION, NODE="skyyprofiles.admin", CATS=CFG_CATS, ROWS=CFG_ROWS,
               FILES=["Skyy_SkyyProfiles/config.properties"],
               NOTE="Slots are 1-6: a way above 6 is not decided yet. Player fixes: /profileadmin info and setclass.",
               RELOAD="ProfCfg.load", KEEP=20, DEFAULTS={"config.properties": CFG_TEXT}, ITEMS=set(_ITEMS) | {DEF_KEEP})
M(cfg, r"""
public static java.util.Map bridge() {
  synchronized (java.lang.System.class) {
    Object o = System.getProperties().get("skyy.bridge");
    if (o == null) { o = new java.util.concurrent.ConcurrentHashMap(); System.getProperties().put("skyy.bridge", o); }
    return (java.util.Map) o;
  }
}""")
M(cfg, r"""
public static void warn(String msg) {
  try { if (LOG != null) LOG.at(java.util.logging.Level.WARNING).log("[SkyyProfiles] " + msg); } catch (Throwable t) { }
}""")
M(cfg, r"""
public static void info(String msg) {
  try { if (LOG != null) LOG.at(java.util.logging.Level.INFO).log("[SkyyProfiles] " + msg); } catch (Throwable t) { }
}""")
# 0.1.1 - player Settings registry (SkyyMenu 0.2+, research/Settings-Spec.md 1.3 / 3.9). No SkyyMenu = no answer = 0.1 behaviour (on).
M(cfg, r"""
public static boolean notifyOn(java.util.UUID u, String key) {
  if (u == null || key == null) return true;
  try {
    Object f = bridge().get("settings:fn:get");
    if (f instanceof java.util.function.Function) {
      Object r = ((java.util.function.Function) f).apply(new Object[] { u, key });
      if (r instanceof Boolean) return ((Boolean) r).booleanValue();
    }
  } catch (Throwable t) { }
  return true;
}""")
M(cfg, r"""
public static void regSetting(String key, String label, String cat, boolean def, String help) {
  try {
    Object[] a = new Object[] { "SkyyProfiles", key, label, cat, Boolean.valueOf(def), help };
    java.util.Map br = bridge();
    br.put("settings:def:" + key, a);
    Object f = br.get("settings:fn:register");
    if (f instanceof java.util.function.Function) ((java.util.function.Function) f).apply(a);
  } catch (Throwable t) { }
}""")
M(cfg, r"""
public static long lng(java.util.Properties p, String k, long d) {
  try { String v = p.getProperty(k); return v == null ? d : Long.parseLong(v.trim()); } catch (Throwable t) { return d; }
}""")
M(cfg, r"""
public static boolean bool(java.util.Properties p, String k, boolean d) {
  String v = p.getProperty(k);
  if (v == null) return d;
  v = v.trim().toLowerCase();
  if (v.equals("true") || v.equals("yes") || v.equals("on") || v.equals("1")) return true;
  if (v.equals("false") || v.equals("no") || v.equals("off") || v.equals("0")) return false;
  return d;
}""")
M(cfg, r"""
public static boolean keep(String id) {
  if (id == null || id.length() == 0) return false;
  String k = KEEP;
  if (k == null || k.length() == 0) return false;
  return ("," + k + ",").indexOf("," + id + ",") >= 0;
}""")
# tmp file + fsync + atomic rename (falls back to a plain replace only where the file system cannot rename atomically).
# Windows refuses to replace a file another handle has open (AccessDeniedException for every in-JVM holder - FileInputStream,
# FileOutputStream, RandomAccessFile, Files.newInputStream - re-tested on the game JRE 2026-09-23; another program holding the file or
# the fresh .tmp - virus scanner, indexer, editor - can surface as a plain FileSystemException sharing violation instead, as SkyyBank's
# integration test saw): the rename is retried 5x 20 ms on any FileSystemException before the write counts as failed
# (AtomicMoveNotSupportedException, also a FileSystemException, is caught first and falls back to a plain replace).
M(cfg, r"""
public static void atomicWrite(java.nio.file.Path f, byte[] data) throws java.io.IOException {
  java.nio.file.Files.createDirectories(f.getParent(), new java.nio.file.attribute.FileAttribute[0]);
  java.nio.file.Path tmp = f.resolveSibling(f.getFileName().toString() + ".tmp");
  java.io.FileOutputStream out = new java.io.FileOutputStream(tmp.toFile());
  try {
    out.write(data);
    out.flush();
    out.getFD().sync();
  } finally { out.close(); }
  java.io.IOException last = null;
  for (int i = 0; i < 5; i++) {
    try {
      java.nio.file.Files.move(tmp, f, new java.nio.file.CopyOption[] { java.nio.file.StandardCopyOption.ATOMIC_MOVE, java.nio.file.StandardCopyOption.REPLACE_EXISTING });
      return;
    } catch (java.nio.file.AtomicMoveNotSupportedException e) {
      java.nio.file.Files.move(tmp, f, new java.nio.file.CopyOption[] { java.nio.file.StandardCopyOption.REPLACE_EXISTING });
      return;
    } catch (java.nio.file.FileSystemException e) {
      last = e;
    }
    try { Thread.sleep(20L); } catch (InterruptedException ie) { }
  }
  throw last;
}""")
M(cfg, r"""
public static String readText(java.nio.file.Path f) throws java.io.IOException {
  return new String(java.nio.file.Files.readAllBytes(f), "UTF-8");
}""")
M(cfg, r"""
public static void appendLine(java.nio.file.Path f, String line) {
  try {
    java.nio.file.Files.createDirectories(f.getParent(), new java.nio.file.attribute.FileAttribute[0]);
    java.nio.file.Files.write(f, (line + "\n").getBytes("UTF-8"), new java.nio.file.OpenOption[] { java.nio.file.StandardOpenOption.CREATE, java.nio.file.StandardOpenOption.APPEND });
  } catch (Throwable t) { warn("could not append to " + f + ": " + t); }
}""")
# 0.1.1: the running values, WITHOUT reading the file (/profileadmin with no arguments; 0.1 re-read the file there, which could undo a
# value the config kit had just set in memory while its 500 ms save was still pending)
M(cfg, r"""
public static String summary() {
  return "maxProfiles=" + MAX_PROFILES + " combatSeconds=" + (COMBAT_MS / 1000L) + " islandOnSwitch=" + ISLAND_ON_SWITCH
    + " perProfileBackpack=" + PER_PROFILE_BACKPACK + " newProfileBackpack=" + NEW_BACKPACK + " keepItems=" + KEEP
    + " promptEveryLogin=" + PROMPT_EVERY_LOGIN + " openDelayMillis=" + OPEN_DELAY_MS;
}""")
# 0.1.1, setup() only, BEFORE load() and before the config kit starts: a config.properties that is still byte for byte the file 0.1 wrote
# by itself (OLD_LINES) was never edited, so it is replaced by the 0.1.1 default file (maxProfiles 4 -> 6, Skyy's new default cap).
# Any other content - an owner's value, a comment, CRLF line ends - is left exactly as it is. null = nothing done.
M(cfg, r"""
public static synchronized String upgradeDefault() {
  try {
    if (FILE == null || !java.nio.file.Files.isRegularFile(FILE, new java.nio.file.LinkOption[0])) return null;
    byte[] have = java.nio.file.Files.readAllBytes(FILE);
    StringBuilder ob = new StringBuilder();
    for (int i = 0; i < OLD_LINES.length; i++) ob.append(OLD_LINES[i]).append("\n");
    if (!java.util.Arrays.equals(have, ob.toString().getBytes("UTF-8"))) return null;
    StringBuilder nb = new StringBuilder();
    for (int i = 0; i < DEFAULT_LINES.length; i++) nb.append(DEFAULT_LINES[i]).append("\n");
    atomicWrite(FILE, nb.toString().getBytes("UTF-8"));
    return "config.properties was the untouched file 0.1 wrote - replaced by the 0.1.1 default (maxProfiles 4 -> " + @MAXDEF@ + ", the new default cap)";
  } catch (Throwable t) { warn("could not check config.properties for the untouched 0.1 default (left as it is): " + t); return null; }
}""".replace("@MAXDEF@", str(DEF_MAX_PROFILES)))
M(cfg, r"""
public static synchronized String load() {
  try {
    java.nio.file.Files.createDirectories(FILE.getParent(), new java.nio.file.attribute.FileAttribute[0]);
    if (!java.nio.file.Files.exists(FILE, new java.nio.file.LinkOption[0])) {
      StringBuilder sb = new StringBuilder();
      for (int i = 0; i < DEFAULT_LINES.length; i++) sb.append(DEFAULT_LINES[i]).append("\n");
      atomicWrite(FILE, sb.toString().getBytes("UTF-8"));
    }
    java.util.Properties p = new java.util.Properties();
    java.io.InputStream in = java.nio.file.Files.newInputStream(FILE, new java.nio.file.OpenOption[0]);
    try { p.load(in); } finally { in.close(); }
    long mp = lng(p, "maxProfiles", (long) MAX_PROFILES);
    if (mp < 1L) mp = 1L;
    if (mp > 6L) mp = 6L;
    long d = lng(p, "openDelayMillis", OPEN_DELAY_MS);
    if (d < 250L) d = 250L;
    if (d > 60000L) d = 60000L;
    long cs = lng(p, "combatSeconds", COMBAT_MS / 1000L);
    if (cs < 0L) cs = 0L;
    if (cs > 600L) cs = 600L;
    long nb = lng(p, "newProfileBackpack", (long) NEW_BACKPACK);
    if (nb < -1L) nb = -1L;
    if (nb > 256L) nb = 256L;
    String k = p.getProperty("keepItems", "");
    StringBuilder ks = new StringBuilder();
    String[] parts = k.split(",");
    for (int i = 0; i < parts.length; i++) {
      String s = parts[i].trim();
      if (s.length() > 0) { if (ks.length() > 0) ks.append(","); ks.append(s); }
    }
    MAX_PROFILES = (int) mp;
    OPEN_DELAY_MS = d;
    COMBAT_MS = cs * 1000L;
    NEW_BACKPACK = (int) nb;
    KEEP = ks.toString();
    PROMPT_EVERY_LOGIN = bool(p, "promptEveryLogin", PROMPT_EVERY_LOGIN);
    ISLAND_ON_SWITCH = bool(p, "islandOnSwitch", ISLAND_ON_SWITCH);
    PER_PROFILE_BACKPACK = bool(p, "perProfileBackpack", PER_PROFILE_BACKPACK);
    return summary();
  } catch (Throwable t) { warn("could not load config: " + t); return "config load failed: " + t; }
}""")

# ================= ProfRoster: class cards = SkyyClasses bridge class:list merged with the fallback table =================
F(ros, "public static final String[] NAMES = %s;" % jarr([c["name"] for c in CLASSES]))
F(ros, "public static final String[] SKILLS = %s;" % jarr([c["skill"] for c in CLASSES]))
F(ros, "public static final String[] DESCS = %s;" % jarr([c["desc"] for c in CLASSES]))
F(ros, "public static final String[] COLORS = %s;" % jarr([c["color"] for c in CLASSES]))
F(ros, "public static final String[] WTEXT = %s;" % jarr([c["weapon_text"] for c in CLASSES]))
F(ros, "public static final String[] ICONS = %s;" % jarr([",".join(c["icons"]) for c in CLASSES]))
F(ros, "public static final String[] ROLES = %s;" % jarr([c["role"] for c in CLASSES]))      # 0.1.2
F(ros, "public static final boolean[] ENABLED = new boolean[] { %s };" % ", ".join("true" if c["enabled"] else "false" for c in CLASSES))
F(ros, "public static final String NEW_ICON = %s;" % jstr(NEW_ICON))
F(ros, "public static final String EXTRA_ICON = %s;" % jstr(EXTRA_ICON))
F(ros, "public static final String EXTRA_ROLE = %s;" % jstr(EXTRA_ROLE))
F(ros, "public static final String EXTRA_DESC = %s;" % jstr(EXTRA_DESC))
M(ros, r"""
public static int indexOf(String s) {
  if (s == null) return -1;
  s = s.trim();
  for (int i = 0; i < NAMES.length; i++) if (NAMES[i].equalsIgnoreCase(s)) return i;
  return -1;
}""")
# one entry = String[] { name, skill, desc, color, weapons text, icons (comma list), "1" selectable / "0" coming later, role (0.1.2) }
# (0.1.2 appends the role at index 7, so every 0.1.1 index keeps its meaning)
M(ros, r"""
public static java.util.ArrayList roster() {
  java.util.ArrayList out = new java.util.ArrayList();
  String list = null;
  try { Object o = @PKG@.ProfCfg.bridge().get("class:list"); if (o instanceof String) list = ((String) o).trim(); } catch (Throwable t) { }
  boolean have = list != null && list.length() > 0;
  String[] parts = have ? list.split(",") : new String[0];
  for (int i = 0; i < NAMES.length; i++) {
    String skill = SKILLS[i];
    boolean on = ENABLED[i];
    if (have) {
      on = false;
      for (int k = 0; k < parts.length; k++) {
        String[] nv = parts[k].split(":");
        if (nv.length > 0 && nv[0].trim().equalsIgnoreCase(NAMES[i])) {
          on = true;
          if (nv.length > 1 && nv[1].trim().length() > 0) skill = nv[1].trim();
        }
      }
    }
    out.add(new String[] { NAMES[i], skill, DESCS[i], COLORS[i], WTEXT[i], ICONS[i], on ? "1" : "0", ROLES[i] });
  }
  if (have) {
    for (int k = 0; k < parts.length; k++) {
      String[] nv = parts[k].split(":");
      String n = nv.length > 0 ? nv[0].trim() : "";
      if (n.length() == 0 || indexOf(n) >= 0) continue;
      String sk = nv.length > 1 ? nv[1].trim() : "";
      out.add(new String[] { n, sk, EXTRA_DESC, "#c9d6e2", "See /class", EXTRA_ICON, "1", EXTRA_ROLE });
    }
  }
  return out;
}""")
M(ros, r"""
public static String[] entry(String name) {
  if (name == null) return null;
  String s = name.trim();
  if (s.length() == 0) return null;
  java.util.ArrayList r = roster();
  for (int i = 0; i < r.size(); i++) {
    String[] c = (String[]) r.get(i);
    if (c[0].equalsIgnoreCase(s) || (c[0] + "s").equalsIgnoreCase(s)) return c;
  }
  return null;
}""")
M(ros, r"""
public static String iconOf(String cls) {
  String[] c = entry(cls);
  if (c == null) return cls == null || cls.trim().length() == 0 ? NEW_ICON : EXTRA_ICON;
  String[] ic = c[5].split(",");
  return ic.length > 0 && ic[0].length() > 0 ? ic[0] : EXTRA_ICON;
}""")
M(ros, r"""
public static String skillOf(String cls) {
  String[] c = entry(cls);
  return c == null ? "" : c[1];
}""")
M(ros, r"""
public static String article(String name) {
  if (name == null || name.length() == 0) return "";
  char c = Character.toUpperCase(name.charAt(0));
  return ("AEIOU".indexOf(c) >= 0 ? "an " : "a ") + name;
}""")
M(ros, r"""
public static String choiceText() {
  java.util.ArrayList r = roster();
  java.util.ArrayList on = new java.util.ArrayList();
  for (int i = 0; i < r.size(); i++) { String[] c = (String[]) r.get(i); if ("1".equals(c[6])) on.add(c[0]); }
  StringBuilder sb = new StringBuilder();
  for (int i = 0; i < on.size(); i++) {
    if (i > 0) sb.append(i == on.size() - 1 ? " or " : ", ");
    sb.append((String) on.get(i));
  }
  return sb.toString();
}""")

# ================= ProfNames: SkyBlock-style fruit names =================
F(nam, "public static final String[] FRUITS = %s;" % jarr(FRUITS))
M(nam, r"""
public static boolean used(java.util.Properties p, String n) {
  if (p == null || n == null) return false;
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
    String v = p.getProperty("p." + i + ".name");
    if (v != null && v.equalsIgnoreCase(n)) return true;
  }
  return false;
}""")
M(nam, r"""
public static String pick(java.util.Properties p, String avoid) {
  java.util.ArrayList free = new java.util.ArrayList();
  for (int i = 0; i < FRUITS.length; i++) {
    if (!used(p, FRUITS[i]) && !FRUITS[i].equals(avoid)) free.add(FRUITS[i]);
  }
  if (free.isEmpty()) {
    for (int i = 0; i < FRUITS.length; i++) if (!used(p, FRUITS[i])) free.add(FRUITS[i]);
  }
  if (free.isEmpty()) return "Profile";
  return (String) free.get(java.util.concurrent.ThreadLocalRandom.current().nextInt(free.size()));
}""")

# ================= ProfStore: the profile list per player (copy-on-write, atomic files), keys, bridge =================
F(sto, "public static java.nio.file.Path DIR;")
F(sto, "public static java.nio.file.Path SWDIR;")
F(sto, "public static java.nio.file.Path INVDIR;")
F(sto, "public static java.nio.file.Path LOGF;")
F(sto, "public static final java.util.concurrent.ConcurrentHashMap DATA = new java.util.concurrent.ConcurrentHashMap();")
F(sto, "public static final java.util.concurrent.ConcurrentHashMap SESSION = new java.util.concurrent.ConcurrentHashMap();")
F(sto, "public static final java.util.concurrent.ConcurrentHashMap BUSY = new java.util.concurrent.ConcurrentHashMap();")
# BROKEN: uuid -> Long time of the last failed read of players/<uuid>.properties (profile changes refused while present)
F(sto, "public static final java.util.concurrent.ConcurrentHashMap BROKEN = new java.util.concurrent.ConcurrentHashMap();")
# JOINED: PlayerConnectEvent handled this session (ReadyTask falls back to the join work when absent)
F(sto, "public static final java.util.concurrent.ConcurrentHashMap JOINED = new java.util.concurrent.ConcurrentHashMap();")
# NOTICE: one chat Message per player, held until the first PlayerReadyEvent of the session
F(sto, "public static final java.util.concurrent.ConcurrentHashMap NOTICE = new java.util.concurrent.ConcurrentHashMap();")
# PENDING: uuid -> Long token of a switch/recovery marker kept 30 s for the engine's player save (ClearLater); never recovered in this JVM
F(sto, "public static final java.util.concurrent.ConcurrentHashMap PENDING = new java.util.concurrent.ConcurrentHashMap();")
F(sto, "public static final java.util.concurrent.atomic.AtomicLong SEQ = new java.util.concurrent.atomic.AtomicLong();")
# every players-file READ runs under the same monitor (ProfStore.class) as every write: on Windows an open reader makes a concurrent
# atomic replace fail with AccessDeniedException (tested on the game JRE), which would fail a commit (= a switch rolls back)
M(sto, r"""
public static synchronized java.util.Properties readPath(java.nio.file.Path f) throws java.io.IOException {
  java.util.Properties p = new java.util.Properties();
  if (java.nio.file.Files.exists(f, new java.nio.file.LinkOption[0])) {
    java.io.InputStream in = java.nio.file.Files.newInputStream(f, new java.nio.file.OpenOption[0]);
    try { p.load(in); } finally { in.close(); }
  }
  return p;
}""")
# null = unreadable (BROKEN gets the time, warned once until a read works again); an empty Properties = no file yet (new player)
M(sto, r"""
public static java.util.Properties readFile(java.util.UUID u) {
  try {
    java.util.Properties p = readPath(DIR.resolve(u.toString() + ".properties"));
    BROKEN.remove(u);
    return p;
  } catch (Throwable t) {
    Object was = BROKEN.put(u, Long.valueOf(System.currentTimeMillis()));
    if (was == null) @PKG@.ProfCfg.warn("could not read players/" + u + ".properties - profile changes for this player are blocked until it is fixed (re-read at every join, on /profileadmin reload, and every 2 s while asked for with no earlier good copy): " + t);
    return null;
  }
}""")
# cache miss: under the store lock. An unreadable file is NOT cached (retried at most every 2 s) - callers get an empty, uncached copy
M(sto, r"""
public static synchronized java.util.Properties loadSlow(java.util.UUID u) {
  java.util.Properties p = (java.util.Properties) DATA.get(u);
  if (p != null) return p;
  Long bad = (Long) BROKEN.get(u);
  if (bad != null && System.currentTimeMillis() - bad.longValue() < 2000L) return new java.util.Properties();
  p = readFile(u);
  if (p == null) return new java.util.Properties();
  DATA.put(u, p);
  return p;
}""")
# hot path: a cached copy (every online player after PlayerConnectEvent) is one ConcurrentHashMap read - no lock, no I/O. Cached copies
# are never mutated after they are cached (copy-on-write in create/setActive/..), so readers need no lock.
M(sto, r"""
public static java.util.Properties load(java.util.UUID u) {
  java.util.Properties p = (java.util.Properties) DATA.get(u);
  if (p != null) return p;
  return loadSlow(u);
}""")
# join preload + /profileadmin reload: REPLACE the cached copy by the file (the entry is never dropped, so there is no moment where the
# key function has to go to disk); an unreadable file keeps the last good copy (BROKEN still blocks changes until a read works)
M(sto, r"""
public static synchronized boolean reload(java.util.UUID u) {
  java.util.Properties p = readFile(u);
  if (p == null) return false;
  DATA.put(u, p);
  return true;
}""")
M(sto, r"""
public static int reloadAll() {
  java.util.HashSet s = new java.util.HashSet(DATA.keySet());
  s.addAll(BROKEN.keySet());
  int n = 0;
  java.util.Iterator it = s.iterator();
  while (it.hasNext()) { if (reload((java.util.UUID) it.next())) n++; }
  return n;
}""")
M(sto, r"""
public static boolean saveProps(java.util.UUID u, java.util.Properties q) {
  try {
    java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();
    q.store(bos, "SkyyProfiles player - profile list (edit only while the player is offline)");
    @PKG@.ProfCfg.atomicWrite(DIR.resolve(u.toString() + ".properties"), bos.toByteArray());
    return true;
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not save players/" + u + ".properties: " + t); return false; }
}""")
M(sto, r"""
public static synchronized boolean commit(java.util.UUID u, java.util.Properties q) {
  if (BROKEN.containsKey(u)) return false;
  if (!saveProps(u, q)) return false;
  DATA.put(u, q);
  return true;
}""")
M(sto, r"""
public static long num(java.util.Properties p, String k) {
  try { String v = p.getProperty(k); return v == null ? 0L : Long.parseLong(v.trim()); } catch (Throwable t) { return 0L; }
}""")
M(sto, r"""
public static boolean exists(java.util.Properties p, String id) {
  if (p == null || id == null) return false;
  return p.getProperty("p." + id + ".name") != null || p.getProperty("p." + id + ".class") != null;
}""")
M(sto, r"""
public static int count(java.util.Properties p) {
  int n = 0;
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) if (exists(p, String.valueOf(i))) n++;
  return n;
}""")
M(sto, r"""
public static String nextId(java.util.Properties p) {
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) if (!exists(p, String.valueOf(i))) return String.valueOf(i);
  return null;
}""")
M(sto, r"""
public static String lowestId(java.util.Properties p) {
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) if (exists(p, String.valueOf(i))) return String.valueOf(i);
  return null;
}""")
M(sto, r"""
public static String keyFor(java.util.UUID u, String id) {
  if (id == null || id.equals("1")) return u.toString();
  return u.toString() + "-p" + id;
}""")
M(sto, r"""
public static String activeOf(java.util.Properties p) {
  if (p == null) return null;
  String a = p.getProperty("active");
  if (a == null) return null;
  a = a.trim();
  return exists(p, a) ? a : null;
}""")
M(sto, r"""
public static String activeId(java.util.UUID u) {
  return activeOf(load(u));
}""")
# the profile whose key profile:fn:key returns: the active one; profiles but no valid "active" (hand edit) -> the lowest id, exactly what
# the join repair then activates; no profile yet (or no readable file) -> null = profile 1's legacy key uuid.toString()
M(sto, r"""
public static String keyId(java.util.Properties p) {
  if (p == null || p.isEmpty()) return null;
  String a = activeOf(p);
  return a != null ? a : lowestId(p);
}""")
# profile:fn:key - hot path for every Skyy mod (see load)
M(sto, r"""
public static String keyOf(java.util.UUID u) {
  return keyFor(u, keyId(load(u)));
}""")
M(sto, r"""
public static synchronized boolean create(java.util.UUID u, String id, String name, String cls, boolean makeActive) {
  java.util.Properties q = (java.util.Properties) load(u).clone();
  if (exists(q, id)) return false;
  String now = String.valueOf(System.currentTimeMillis());
  q.setProperty("p." + id + ".name", name);
  q.setProperty("p." + id + ".class", cls == null ? "" : cls);
  q.setProperty("p." + id + ".created", now);
  q.setProperty("p." + id + ".lastPlayed", now);
  if (makeActive) q.setProperty("active", id);
  q.setProperty("epoch", String.valueOf(num(q, "epoch") + 1L));
  return commit(u, q);
}""")
M(sto, r"""
public static synchronized boolean setActive(java.util.UUID u, String from, String to) {
  java.util.Properties q = (java.util.Properties) load(u).clone();
  if (!exists(q, to)) return false;
  String now = String.valueOf(System.currentTimeMillis());
  q.setProperty("active", to);
  if (from != null && exists(q, from) && !from.equals(to)) {
    q.setProperty("p." + from + ".lastPlayed", now);
    q.setProperty("p." + from + ".inv", "1");
  }
  q.remove("p." + to + ".inv");
  q.setProperty("p." + to + ".lastPlayed", now);
  q.setProperty("epoch", String.valueOf(num(q, "epoch") + 1L));
  q.setProperty("switches", String.valueOf(num(q, "switches") + 1L));
  return commit(u, q);
}""")
M(sto, r"""
public static synchronized boolean setClass(java.util.UUID u, String id, String cls) {
  java.util.Properties q = (java.util.Properties) load(u).clone();
  if (!exists(q, id)) return false;
  q.setProperty("p." + id + ".class", cls);
  if (id.equals(activeOf(q))) q.setProperty("epoch", String.valueOf(num(q, "epoch") + 1L));
  return commit(u, q);
}""")
M(sto, r"""
public static synchronized void touch(java.util.UUID u) {
  java.util.Properties p = load(u);
  String a = activeOf(p);
  if (a == null) return;
  java.util.Properties q = (java.util.Properties) p.clone();
  q.setProperty("p." + a + ".lastPlayed", String.valueOf(System.currentTimeMillis()));
  commit(u, q);
}""")
M(sto, r"""
public static synchronized void noteLogin(java.util.UUID u, String username) {
  java.util.Properties p = load(u);
  if (username == null || username.equals(p.getProperty("username"))) return;
  java.util.Properties q = (java.util.Properties) p.clone();
  q.setProperty("username", username);
  commit(u, q);
}""")
M(sto, r"""
public static synchronized void markPrompted(java.util.UUID u) {
  java.util.Properties p = load(u);
  if ("1".equals(p.getProperty("prompted"))) return;
  java.util.Properties q = (java.util.Properties) p.clone();
  q.setProperty("prompted", "1");
  commit(u, q);
}""")
M(sto, r"""
public static String listText(java.util.Properties p) {
  StringBuilder sb = new StringBuilder();
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
    String id = String.valueOf(i);
    if (!exists(p, id)) continue;
    if (sb.length() > 0) sb.append(",");
    sb.append(id).append(":").append(p.getProperty("p." + id + ".name", "")).append(":").append(p.getProperty("p." + id + ".class", ""));
  }
  return sb.toString();
}""")
# contract bridge keys; epoch goes LAST so a consumer that sees the new epoch also sees the new key/class/name.
# One monitor (ProfPub.class, never held across disk I/O) and the state is read INSIDE it: publishes from different threads (join,
# switch, admin) can no longer interleave and leave an older epoch/key over a newer one. No cached state (unreadable file and no
# earlier good copy) -> nothing is published (a missing epoch is less wrong than a made-up "no profile").
M(pub, r"""
public static synchronized void publishLocked(java.util.UUID u, java.util.Map b) {
  java.util.Properties p = (java.util.Properties) @PKG@.ProfStore.DATA.get(u);
  if (p == null) return;
  String us = u.toString();
  String a = @PKG@.ProfStore.keyId(p);
  b.put("profile:key:" + us, @PKG@.ProfStore.keyFor(u, a));
  if (a == null) {
    b.remove("profile:" + us);
    b.remove("profile:class:" + us);
    b.remove("profile:name:" + us);
    b.remove("profile:list:" + us);
  } else {
    b.put("profile:" + us, a);
    String c = p.getProperty("p." + a + ".class", "");
    if (c.length() > 0) b.put("profile:class:" + us, c); else b.remove("profile:class:" + us);
    String n = p.getProperty("p." + a + ".name", "");
    if (n.length() > 0) b.put("profile:name:" + us, n); else b.remove("profile:name:" + us);
    b.put("profile:list:" + us, @PKG@.ProfStore.listText(p));
  }
  b.put("profile:epoch:" + us, Long.valueOf(@PKG@.ProfStore.num(p, "epoch")));
}""")
M(sto, r"""
public static void publish(java.util.UUID u) {
  load(u);
  @PKG@.ProfPub.publishLocked(u, @PKG@.ProfCfg.bridge());
}""")
# profile:busy:<uuid> = the live vanilla inventory may not belong to the active profile right now (switch running / recovery pending)
M(sto, r"""
public static void busy(java.util.UUID u, boolean on) {
  try {
    if (on) @PKG@.ProfCfg.bridge().put("profile:busy:" + u.toString(), Boolean.TRUE);
    else @PKG@.ProfCfg.bridge().remove("profile:busy:" + u.toString());
  } catch (Throwable t) { }
}""")
# chat for a joining player: held until the first PlayerReadyEvent of the session (ReadyTask sends it), sent at once when that already
# happened. Put-then-check here, remove in ReadyTask: whichever side removes it sends it - never lost, never twice.
M(sto, r"""
public static void notice(@PR@ pr, @MSG@ m) {
  java.util.UUID u = pr.getUuid();
  NOTICE.put(u, m);
  if (SESSION.containsKey(u)) {
    Object o = NOTICE.remove(u);
    if (o != null) pr.sendMessage((@MSG@) o);
  }
}""")
M(sto, r"""
public static String resolveId(java.util.UUID u, String arg) {
  if (arg == null) return null;
  String s = arg.trim();
  java.util.Properties p = load(u);
  if (exists(p, s)) return s;
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
    String id = String.valueOf(i);
    if (exists(p, id) && s.equalsIgnoreCase(p.getProperty("p." + id + ".name", ""))) return id;
  }
  return null;
}""")
M(sto, r"""
public static java.util.UUID resolve(String s) {
  if (s == null) return null;
  s = s.trim();
  if (s.length() == 0) return null;
  try {
    java.util.Iterator it = @UNI@.get().getPlayers().iterator();
    while (it.hasNext()) {
      @PR@ p = (@PR@) it.next();
      if (p != null && p.getUsername() != null && p.getUsername().equalsIgnoreCase(s)) return p.getUuid();
    }
  } catch (Throwable t) { }
  try { return java.util.UUID.fromString(s); } catch (Throwable t) { }
  java.util.UUID found = null;
  try {
    java.nio.file.DirectoryStream ds = java.nio.file.Files.newDirectoryStream(DIR, "*.properties");
    try {
      java.util.Iterator it2 = ds.iterator();
      while (found == null && it2.hasNext()) {
        java.nio.file.Path f = (java.nio.file.Path) it2.next();
        java.util.Properties q = readPath(f);
        if (s.equalsIgnoreCase(q.getProperty("username", ""))) {
          String fn = f.getFileName().toString();
          found = java.util.UUID.fromString(fn.substring(0, fn.length() - 11));
        }
      }
    } finally { ds.close(); }
  } catch (Throwable t) { }
  return found;
}""")
M(sto, r"""
public static String fmtAgo(long t) {
  if (t <= 0L) return "never";
  long s = (System.currentTimeMillis() - t) / 1000L;
  if (s < 60L) return "just now";
  long m = s / 60L;
  if (m < 60L) return m + " min ago";
  long h = m / 60L;
  if (h < 48L) return h + " h ago";
  return (h / 24L) + " days ago";
}""")
M(sto, r"""
public static String fmtDate(long t) {
  if (t <= 0L) return "unknown";
  try { return new java.text.SimpleDateFormat("yyyy-MM-dd").format(new java.util.Date(t)); } catch (Throwable x) { return "unknown"; }
}""")
M(sto, r"""
public static String describe(java.util.UUID u) {
  java.util.Properties p = load(u);
  String act = activeOf(p);
  StringBuilder sb = new StringBuilder();
  int n = 0;
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
    String id = String.valueOf(i);
    if (!exists(p, id)) continue;
    if (n > 0) sb.append(" | ");
    n++;
    String c = p.getProperty("p." + id + ".class", "");
    sb.append(id).append(" ").append(p.getProperty("p." + id + ".name", "?")).append(" - ").append(c.length() == 0 ? "no class" : c);
    if (id.equals(act)) sb.append(" - ACTIVE");
    else sb.append(" - last played ").append(fmtAgo(num(p, "p." + id + ".lastPlayed")));
  }
  if (n == 0) return "no profiles yet";
  return sb.toString() + " | slots " + n + "/" + @PKG@.ProfCfg.MAX_PROFILES;
}""")
M(sto, r"""
public static void log(String line) {
  @PKG@.ProfCfg.appendLine(LOGF, java.time.Instant.now().toString() + " " + line);
}""")

# ================= ProfKit (0.1.2): tell SkyyClasses a profile picked its class, so the class kit follows (spec 4.3) =================
# class:fn:kitnew (SkyyClasses 0.1.6+) = Function apply(Object[] { UUID player, String pkey, String className }) -> Boolean: TRUE =
# recorded (pending, or off while kits are off) or the profile already has a kit state; FALSE = bad arguments / class file unusable.
# It runs on the caller's thread (a small synchronous file write under SkyyClasses' own lock, never a call into another mod), so the
# callers invoke it OUTSIDE every SkyyProfiles monitor. Missing = SkyyClasses absent or older = nothing to do. Never throws.
# A miss (answer other than TRUE, or an exception) = a KIT-MISSED line in switches.log EVERY time (review fix: the admin's list of who
# needs /classadmin kit) + ONE console warning per JVM (spec 4.3) that points at that list.
# Compiled after ProfStore (log -> switches.log) and before ProfSwitch / ProfilePage / the setclass command (the callers).
F(pkit, "public static final java.util.concurrent.atomic.AtomicBoolean WARNED = new java.util.concurrent.atomic.AtomicBoolean(false);")
M(pkit, r"""
public static java.util.function.Function fn() {
  try {
    Object f = @PKG@.ProfCfg.bridge().get("class:fn:kitnew");
    if (f instanceof java.util.function.Function) return (java.util.function.Function) f;
  } catch (Throwable t) { }
  return null;
}""")
M(pkit, r"""
public static boolean available() {
  return fn() != null;
}""")
M(pkit, r"""
public static void warnOnce(String msg) {
  if (WARNED.compareAndSet(false, true)) @PKG@.ProfCfg.warn(msg + " (this warning is logged once per server start)");
}""")
M(pkit, r"""
public static void missed(java.util.UUID u, String key, String cls, String why) {
  try { @PKG@.ProfStore.log("KIT-MISSED " + u + " key " + key + " class " + cls + " - " + why); } catch (Throwable t) { }
  warnOnce("the class kit of a new profile was not recorded: " + u + " (profile key " + key + ", class " + cls + ") - " + why + " - /classadmin kit <player> can hand it out; switches.log lists every miss (KIT-MISSED lines)");
}""")
M(pkit, r"""
public static boolean kitNew(java.util.UUID u, String key, String cls) {
  if (u == null || key == null || key.length() == 0 || cls == null || cls.trim().length() == 0) return false;
  java.util.function.Function f = fn();
  if (f == null) return false;
  String why = null;
  try {
    Object r = f.apply(new Object[] { u, key, cls });
    if (Boolean.TRUE.equals(r)) return true;
    why = "class:fn:kitnew answered " + r;
  } catch (Throwable t) {
    why = "class:fn:kitnew failed: " + t;
  }
  missed(u, key, cls, why);
  return false;
}""")

# ================= KeyFn: bridge profile:fn:key =================
kfn.addInterface(pool.get("java.util.function.Function"))
C(kfn, "public KeyFn() { }")
M(kfn, r"""
public Object apply(Object o) {
  try { if (o instanceof java.util.UUID) return @PKG@.ProfStore.keyOf((java.util.UUID) o); } catch (Throwable t) { }
  return o == null ? null : String.valueOf(o);
}""")

# ================= ProfInv: vanilla inventory snapshot / clear / restore (WORLD THREAD ONLY) =================
F(inv, "public static final String[] SECTIONS = new String[] { \"hotbar\", \"storage\", \"backpack\", \"armor\", \"utility\", \"tools\" };")
M(inv, r"""
public static @CT@ typeOf(int i) {
  if (i == 0) return @HOT@.getComponentType();
  if (i == 1) return @STO@.getComponentType();
  if (i == 2) return @BAK@.getComponentType();
  if (i == 3) return @ARM@.getComponentType();
  if (i == 4) return @UTI@.getComponentType();
  if (i == 5) return @TOO@.getComponentType();
  return null;
}""")
M(inv, r"""
public static @IC@ section(@ST@ st, @REF@ ref, int i) {
  @CT@ t = typeOf(i);
  if (t == null) return null;
  @INVC@ c = (@INVC@) st.getComponent(ref, t);
  return c == null ? null : c.getInventory();
}""")
M(inv, r"""
public static int secIndex(String name) {
  if (name == null) return -1;
  for (int i = 0; i < SECTIONS.length; i++) if (SECTIONS[i].equals(name)) return i;
  return -1;
}""")
M(inv, r"""
public static int intOf(org.bson.BsonDocument d, String k, int def) {
  try { org.bson.BsonValue v = (org.bson.BsonValue) d.get(k); if (v != null && v.isNumber()) return v.asNumber().intValue(); } catch (Throwable t) { }
  return def;
}""")
M(inv, r"""
public static double dblOf(org.bson.BsonDocument d, String k) {
  try { org.bson.BsonValue v = (org.bson.BsonValue) d.get(k); if (v != null && v.isNumber()) return v.asNumber().doubleValue(); } catch (Throwable t) { }
  return 0.0;
}""")
M(inv, r"""
public static String strOf(org.bson.BsonDocument d, String k) {
  try { org.bson.BsonValue v = (org.bson.BsonValue) d.get(k); if (v != null && v.isString()) return v.asString().getValue(); } catch (Throwable t) { }
  return null;
}""")
M(inv, r"""
public static org.bson.BsonDocument slotDoc(int slot, @IS@ s) {
  org.bson.BsonDocument d = new org.bson.BsonDocument();
  d.put("slot", new org.bson.BsonInt32(slot));
  d.put("id", new org.bson.BsonString(s.getItemId()));
  d.put("qty", new org.bson.BsonInt32(s.getQuantity()));
  d.put("durability", new org.bson.BsonDouble(s.getDurability()));
  d.put("maxDurability", new org.bson.BsonDouble(s.getMaxDurability()));
  d.put("quality", new org.bson.BsonInt32(s.getQualityIndex()));
  d.put("overrideAnim", new org.bson.BsonBoolean(s.getOverrideDroppedItemAnimation()));
  org.bson.BsonDocument m = s.getMetadata();
  if (m != null) d.put("meta", (org.bson.BsonDocument) m.clone());
  try {
    org.bson.BsonValue enc = ((@CODEC@) @IS@.CODEC).encode(s);
    if (enc != null) d.put("stack", enc);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("ItemStack.CODEC could not encode " + s.getItemId() + " (explicit fields kept): " + t); }
  return d;
}""")
M(inv, r"""
public static org.bson.BsonDocument header(java.util.UUID u, String id, String key) {
  org.bson.BsonDocument doc = new org.bson.BsonDocument();
  doc.put("format", new org.bson.BsonInt32(1));
  doc.put("mod", new org.bson.BsonString("SkyyProfiles"));
  doc.put("uuid", new org.bson.BsonString(u.toString()));
  doc.put("profile", new org.bson.BsonString(id == null ? "1" : id));
  doc.put("key", new org.bson.BsonString(key));
  doc.put("savedAt", new org.bson.BsonInt64(System.currentTimeMillis()));
  return doc;
}""")
M(inv, r"""
public static org.bson.BsonDocument capture(@ST@ st, @REF@ ref, java.util.UUID u, String id, String key) {
  org.bson.BsonDocument doc = header(u, id, key);
  org.bson.BsonArray secs = new org.bson.BsonArray();
  int total = 0;
  for (int i = 0; i < SECTIONS.length; i++) {
    @IC@ c = section(st, ref, i);
    int cap = 0;
    if (c != null) cap = c.getCapacity();
    org.bson.BsonDocument sd = new org.bson.BsonDocument();
    sd.put("name", new org.bson.BsonString(SECTIONS[i]));
    sd.put("capacity", new org.bson.BsonInt32(cap));
    org.bson.BsonArray slots = new org.bson.BsonArray();
    for (int k = 0; k < cap; k++) {
      @IS@ s = c.getItemStack((short) k);
      if (s == null || s.isEmpty()) continue;
      if (@PKG@.ProfCfg.keep(s.getItemId())) continue;
      slots.add(slotDoc(k, s));
      total++;
    }
    sd.put("slots", slots);
    secs.add(sd);
    if (i == 2 && c != null) doc.put("backpackCapacity", new org.bson.BsonInt32(cap));
  }
  doc.put("sections", secs);
  doc.put("count", new org.bson.BsonInt32(total));
  return doc;
}""")
M(inv, r"""
public static int slotCount(org.bson.BsonDocument doc) {
  int n = 0;
  if (doc == null) return -1;
  org.bson.BsonArray secs = doc.getArray("sections", new org.bson.BsonArray());
  for (int i = 0; i < secs.size(); i++) {
    org.bson.BsonValue v = (org.bson.BsonValue) secs.get(i);
    if (v == null || !v.isDocument()) continue;
    n = n + v.asDocument().getArray("slots", new org.bson.BsonArray()).size();
  }
  return n;
}""")
M(inv, r"""
public static java.nio.file.Path fileOf(String key) {
  return @PKG@.ProfStore.INVDIR.resolve(key + ".json");
}""")
M(inv, r"""
public static String toJson(org.bson.BsonDocument d) {
  org.bson.json.JsonWriterSettings s = org.bson.json.JsonWriterSettings.builder().outputMode(org.bson.json.JsonMode.EXTENDED).indent(true).build();
  return d.toJson(s);
}""")
M(inv, r"""
public static org.bson.BsonDocument read(String key) {
  java.nio.file.Path f = fileOf(key);
  try {
    if (!java.nio.file.Files.exists(f, new java.nio.file.LinkOption[0])) return null;
    return org.bson.BsonDocument.parse(@PKG@.ProfCfg.readText(f));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not read inventory file " + f + ": " + t); return null; }
}""")
M(inv, r"""
public static boolean write(String key, org.bson.BsonDocument doc) {
  java.nio.file.Path f = fileOf(key);
  try {
    @PKG@.ProfCfg.atomicWrite(f, toJson(doc).getBytes("UTF-8"));
    org.bson.BsonDocument back = read(key);
    if (back == null || slotCount(back) != slotCount(doc)) { @PKG@.ProfCfg.warn("inventory file " + f + " did not read back correctly"); return false; }
    return true;
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not write inventory file " + f + ": " + t); return false; }
}""")
M(inv, r"""
public static org.bson.BsonDocument empty(java.util.UUID u, String id, String key) {
  org.bson.BsonDocument doc = header(u, id, key);
  doc.put("sections", new org.bson.BsonArray());
  doc.put("count", new org.bson.BsonInt32(0));
  doc.put("fresh", new org.bson.BsonBoolean(true));
  if (@PKG@.ProfCfg.NEW_BACKPACK >= 0) doc.put("backpackCapacity", new org.bson.BsonInt32(@PKG@.ProfCfg.NEW_BACKPACK));
  return doc;
}""")
# engine codec first (exact engine persistence semantics), explicit fields as the fallback
M(inv, r"""
public static @IS@ stackOf(org.bson.BsonDocument d) {
  String id = strOf(d, "id");
  int qty = intOf(d, "qty", 0);
  if (id == null || id.length() == 0 || qty <= 0) return null;
  try {
    org.bson.BsonValue enc = (org.bson.BsonValue) d.get("stack");
    if (enc != null && enc.isDocument()) {
      Object o = ((@CODEC@) @IS@.CODEC).decode(enc);
      if (o instanceof @IS@) {
        @IS@ s = (@IS@) o;
        if (!s.isEmpty() && id.equals(s.getItemId()) && s.getQuantity() == qty) return s;
      }
    }
  } catch (Throwable t) { }
  org.bson.BsonDocument meta = null;
  try { org.bson.BsonValue mv = (org.bson.BsonValue) d.get("meta"); if (mv != null && mv.isDocument()) meta = mv.asDocument(); } catch (Throwable t) { }
  @IS@ s2 = null;
  try { s2 = new @IS@(id, qty, dblOf(d, "durability"), dblOf(d, "maxDurability"), intOf(d, "quality", 0), meta); }
  catch (Throwable t) { @PKG@.ProfCfg.warn("saved item " + id + " x" + qty + " cannot be rebuilt (unknown item?): " + t); return null; }
  try {
    org.bson.BsonValue a = (org.bson.BsonValue) d.get("overrideAnim");
    if (a != null && a.isBoolean() && a.asBoolean().getValue()) s2.setOverrideDroppedItemAnimation(true);
  } catch (Throwable t) { }
  return s2;
}""")
M(inv, r"""
public static boolean hasItem(@ST@ st, @REF@ ref, String id) {
  for (int i = 0; i < SECTIONS.length; i++) {
    @IC@ c = section(st, ref, i);
    if (c == null) continue;
    int cap = c.getCapacity();
    for (int k = 0; k < cap; k++) {
      @IS@ s = c.getItemStack((short) k);
      if (s != null && !s.isEmpty() && id.equals(s.getItemId())) return true;
    }
  }
  return false;
}""")
# empties every section except keepItems; true only when every other slot is verified empty
M(inv, r"""
public static boolean clearAll(@ST@ st, @REF@ ref) {
  boolean ok = true;
  for (int i = 0; i < SECTIONS.length; i++) {
    @IC@ c = section(st, ref, i);
    if (c == null) continue;
    int cap = c.getCapacity();
    boolean kept = false;
    for (int k = 0; k < cap; k++) {
      @IS@ s = c.getItemStack((short) k);
      if (s != null && !s.isEmpty() && @PKG@.ProfCfg.keep(s.getItemId())) kept = true;
    }
    if (!kept) {
      try { c.clear(); } catch (Throwable t) { @PKG@.ProfCfg.warn("clear of " + SECTIONS[i] + " failed, clearing slot by slot: " + t); }
    }
    for (int k = 0; k < cap; k++) {
      @IS@ s = c.getItemStack((short) k);
      if (s == null || s.isEmpty() || @PKG@.ProfCfg.keep(s.getItemId())) continue;
      try { c.removeItemStackFromSlot((short) k, false); } catch (Throwable t) { }
      @IS@ s2 = c.getItemStack((short) k);
      if (s2 != null && !s2.isEmpty()) { ok = false; @PKG@.ProfCfg.warn("could not empty " + SECTIONS[i] + " slot " + k + " (" + s2.getItemId() + ")"); }
    }
  }
  return ok;
}""")
M(inv, r"""
public static void give(@ST@ st, @REF@ ref, @PLA@ player, @IS@ s, java.util.UUID u) {
  try {
    @SIC@.addOrDropItemStack(st, ref, player.getInventory().getCombinedStorageHotbarBackpack(), s);
  } catch (Throwable t) {
    @PKG@.ProfCfg.warn("could not give " + s.getItemId() + " x" + s.getQuantity() + " to " + u + ": " + t);
    try { @PKG@.ProfStore.log("LOST? " + u + " " + toJson(slotDoc(-1, s)).replace('\n', ' ')); } catch (Throwable t2) { }
  }
}""")
# returns { placed, moved (not in its old slot -> storage first, dropped if full), skipped }
# rollbackMode: a slot that still holds the identical stack (it could not be cleared) counts as placed - never duplicated
M(inv, r"""
public static int[] loadInto(@ST@ st, @REF@ ref, @PLA@ player, org.bson.BsonDocument doc, java.util.UUID u, boolean rollbackMode) {
  int placed = 0;
  int moved = 0;
  int skipped = 0;
  if (@PKG@.ProfCfg.PER_PROFILE_BACKPACK && doc.containsKey("backpackCapacity")) {
    int want = intOf(doc, "backpackCapacity", -1);
    @BAK@ bp = (@BAK@) st.getComponent(ref, @BAK@.getComponentType());
    if (bp != null && want >= 0 && want <= 32000 && want != bp.getInventory().getCapacity()) {
      java.util.ArrayList over = new java.util.ArrayList();
      bp.resize((short) want, over);
      for (int k = 0; k < over.size(); k++) {
        @IS@ os = (@IS@) over.get(k);
        if (os != null && !os.isEmpty()) { give(st, ref, player, os, u); moved++; }
      }
    }
  }
  org.bson.BsonArray secs = doc.getArray("sections", new org.bson.BsonArray());
  for (int i = 0; i < secs.size(); i++) {
    org.bson.BsonValue sv = (org.bson.BsonValue) secs.get(i);
    if (sv == null || !sv.isDocument()) continue;
    org.bson.BsonDocument sd = sv.asDocument();
    int si = secIndex(strOf(sd, "name"));
    @IC@ c = si < 0 ? null : section(st, ref, si);
    org.bson.BsonArray slots = sd.getArray("slots", new org.bson.BsonArray());
    for (int k = 0; k < slots.size(); k++) {
      org.bson.BsonValue v = (org.bson.BsonValue) slots.get(k);
      if (v == null || !v.isDocument()) { skipped++; continue; }
      org.bson.BsonDocument d = v.asDocument();
      @IS@ s = stackOf(d);
      if (s == null) { skipped++; @PKG@.ProfCfg.warn("unreadable saved slot for " + u + ": " + d.toJson()); @PKG@.ProfStore.log("LOST? " + u + " unreadable slot " + d.toJson()); continue; }
      if (@PKG@.ProfCfg.keep(s.getItemId()) && hasItem(st, ref, s.getItemId())) { skipped++; continue; }
      int slot = intOf(d, "slot", -1);
      boolean done = false;
      if (c != null && slot >= 0 && slot < c.getCapacity()) {
        @IS@ cur = c.getItemStack((short) slot);
        if (cur == null || cur.isEmpty()) {
          try { c.setItemStackForSlot((short) slot, s, false); } catch (Throwable t) { }
          @IS@ chk = c.getItemStack((short) slot);
          if (chk != null && !chk.isEmpty() && s.getItemId().equals(chk.getItemId())) {
            int rest = s.getQuantity() - chk.getQuantity();
            if (rest <= 0) done = true; else s = s.withQuantity(rest);
          }
        } else if (rollbackMode && s.getItemId().equals(cur.getItemId()) && cur.getQuantity() == s.getQuantity()) {
          done = true;
        }
      }
      if (done) placed++;
      else { give(st, ref, player, s, u); moved++; }
    }
  }
  return new int[] { placed, moved, skipped };
}""")

# ================= ProfSwitch: checks, the switch transaction, creation, crash recovery =================
M(sw, r"""
public static long ago(java.time.Instant then, java.time.Instant now) {
  if (then == null || now == null) return -1L;
  try {
    long ms = java.time.Duration.between(then, now).toMillis();
    return ms < 0L ? -1L : ms;
  } catch (Throwable t) { return -1L; }
}""")
M(sw, r"""
public static String refuse(@ST@ st, @REF@ ref, @PLA@ player) {
  try {
    if (st.getComponent(ref, @DEATH@.getComponentType()) != null) return "You cannot switch profiles while dead.";
  } catch (Throwable t) { }
  try {
    @DDC@ dc = (@DDC@) st.getComponent(ref, @DDC@.getComponentType());
    if (dc != null && @PKG@.ProfCfg.COMBAT_MS > 0L) {
      @TR@ tr = (@TR@) st.getResource(@TR@.getResourceType());
      java.time.Instant now = tr == null ? null : tr.getNow();
      long a = ago(dc.getLastDamageTime(), now);
      long b = ago(dc.getLastCombatAction(), now);
      long m = -1L;
      if (a >= 0L) m = a;
      if (b >= 0L && (m < 0L || b < m)) m = b;
      if (m >= 0L && m < @PKG@.ProfCfg.COMBAT_MS) {
        long left = (@PKG@.ProfCfg.COMBAT_MS - m + 999L) / 1000L;
        return "You are in combat - wait " + left + " s before switching profiles.";
      }
    }
  } catch (Throwable t) { }
  try {
    Object cp = player.getPageManager().getCustomPage();
    if (cp != null && !(cp instanceof @PKG@.ProfilePage)) return "Close the open menu first, then switch profiles.";
  } catch (Throwable t) { }
  return null;
}""")
M(sw, r"""
public static java.nio.file.Path markerFile(java.util.UUID u) {
  return @PKG@.ProfStore.SWDIR.resolve(u.toString() + ".properties");
}""")
M(sw, r"""
public static boolean storeMarker(java.util.UUID u, java.util.Properties m) {
  try {
    java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();
    m.store(bos, "SkyyProfiles switch marker - saved = finish or roll back at join, done = delete, failed = an admin restores by hand");
    @PKG@.ProfCfg.atomicWrite(markerFile(u), bos.toByteArray());
    return true;
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not write the switch marker for " + u + ": " + t); return false; }
}""")
M(sw, r"""
public static java.util.Properties readMarker(java.util.UUID u) {
  java.nio.file.Path f = markerFile(u);
  try {
    if (!java.nio.file.Files.exists(f, new java.nio.file.LinkOption[0])) return null;
    java.util.Properties m = new java.util.Properties();
    java.io.InputStream in = java.nio.file.Files.newInputStream(f, new java.nio.file.OpenOption[0]);
    try { m.load(in); } finally { in.close(); }
    return m;
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not read the switch marker " + f + ": " + t); return null; }
}""")
M(sw, r"""
public static boolean writeMarker(java.util.UUID u, String from, String to, String fromKey, String toKey, boolean toHad) {
  java.util.Properties m = new java.util.Properties();
  m.setProperty("from", from);
  m.setProperty("to", to);
  m.setProperty("fromKey", fromKey);
  m.setProperty("toKey", toKey);
  m.setProperty("toHadFile", toHad ? "1" : "0");
  m.setProperty("stage", "saved");
  m.setProperty("at", String.valueOf(System.currentTimeMillis()));
  return storeMarker(u, m);
}""")
M(sw, r"""
public static void clearMarker(java.util.UUID u) {
  java.nio.file.Path f = markerFile(u);
  Throwable last = null;
  for (int i = 0; i < 5; i++) {
    try { java.nio.file.Files.deleteIfExists(f); return; } catch (Throwable t) { last = t; }
    try { Thread.sleep(20L); } catch (InterruptedException ie) { }
  }
  @PKG@.ProfCfg.warn("could not delete " + f + " - marking it done: " + last);
  java.util.Properties m = new java.util.Properties();
  m.setProperty("stage", "done");
  storeMarker(u, m);
}""")

# ================= ClearLater: a finished switch / clean recovery keeps its marker 30 s =================
# The engine saves a player's inventory at most every 10 s (PlayerSavingSystems$TickingSystem, only when something changed; the write
# itself is async). A crash inside that window reloads the PRE-switch inventory while players/<uuid>.properties already says the new
# profile: one profile's items duplicated, the other's lost. With the marker still there the next join rolls forward (clear + load the
# active profile's snapshot) instead. 30 s = 3 save periods. Token (PENDING) = only the latest switch's clear may delete; a new switch
# that writes its own marker drops the old token. The clear takes BUSY so it never runs beside a switch or a recovery of that player
# (and never deletes a stage=failed marker). Plugin shutdown (graceful stop: the engine saves every online player) clears at once.
clr.addInterface(pool.get("java.lang.Runnable"))
F(clr, "public java.util.UUID u;")
F(clr, "public long token;")
F(clr, "public int tries;")
C(clr, "public ClearLater(java.util.UUID u, long token) { this.u = u; this.token = token; this.tries = 0; }")
M(clr, r"""
public void run() {
  try {
    Object cur = @PKG@.ProfStore.PENDING.get(this.u);
    if (cur == null || ((Long) cur).longValue() != this.token) return;
    if (@PKG@.ProfStore.BUSY.putIfAbsent(this.u, Boolean.TRUE) != null) {
      if (++this.tries < 600) @HSV@.SCHEDULED_EXECUTOR.schedule(this, 1000L, java.util.concurrent.TimeUnit.MILLISECONDS);
      return;
    }
    try {
      if (@PKG@.ProfStore.PENDING.remove(this.u, Long.valueOf(this.token))) {
        java.util.Properties m = @PKG@.ProfSwitch.readMarker(this.u);
        if (m != null && "saved".equals(m.getProperty("stage", ""))) @PKG@.ProfSwitch.clearMarker(this.u);
      }
    } catch (Throwable t) { @PKG@.ProfCfg.warn("could not clear the switch marker of " + this.u + ": " + t); }
    @PKG@.ProfStore.BUSY.remove(this.u);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("marker clear failed: " + t); }
}""")
M(clr, r"""
public static void arm(java.util.UUID u) {
  long t = @PKG@.ProfStore.SEQ.incrementAndGet();
  @PKG@.ProfStore.PENDING.put(u, Long.valueOf(t));
  try { @HSV@.SCHEDULED_EXECUTOR.schedule(new @PKG@.ClearLater(u, t), 30000L, java.util.concurrent.TimeUnit.MILLISECONDS); }
  catch (Throwable x) { @PKG@.ProfStore.PENDING.remove(u); @PKG@.ProfSwitch.clearMarker(u); }
}""")
M(clr, r"""
public static int clearAllNow() {
  java.util.ArrayList ks = new java.util.ArrayList(@PKG@.ProfStore.PENDING.keySet());
  int n = 0;
  for (int i = 0; i < ks.size(); i++) {
    java.util.UUID u = (java.util.UUID) ks.get(i);
    if (@PKG@.ProfStore.PENDING.remove(u) != null) { @PKG@.ProfSwitch.clearMarker(u); n++; }
  }
  return n;
}""")
M(sw, r"""
public static void failMarker(java.util.UUID u) {
  java.util.Properties m = readMarker(u);
  if (m == null) m = new java.util.Properties();
  m.setProperty("stage", "failed");
  storeMarker(u, m);
}""")
# same, with a line for the admin (what already happened, what to check) - shown by recoverPlan at every later join
M(sw, r"""
public static void failMarkerNote(java.util.UUID u, String note) {
  java.util.Properties m = readMarker(u);
  if (m == null) m = new java.util.Properties();
  m.setProperty("stage", "failed");
  m.setProperty("note", note);
  storeMarker(u, m);
}""")
# a FAILED marker = an admin must look first: no new switch may overwrite the marker or the snapshots it points at
M(sw, r"""
public static String markerBlock(java.util.UUID u) {
  java.util.Properties m = readMarker(u);
  if (m == null || !"failed".equals(m.getProperty("stage", ""))) return null;
  return "An earlier profile switch of yours needs an admin first - your items are saved on the server. Profile switching is paused until then. Please tell an admin.";
}""")
M(sw, r"""
public static void closeOurPage(@REF@ ref, @ST@ st, @PLA@ player) {
  try {
    Object cp = player.getPageManager().getCustomPage();
    if (cp instanceof @PKG@.ProfilePage) player.getPageManager().setPage(ref, st, @PGE@.None);
  } catch (Throwable t) { }
}""")
# the player's own /island (SkyyIslands sends them to the ACTIVE profile's island); fallback = default world spawn (SkyyIslands HubCmd)
M(sw, r"""
public static void travel(@ST@ st, @REF@ ref, @PR@ pr) {
  if (!@PKG@.ProfCfg.ISLAND_ON_SWITCH) return;
  try {
    if (@CMGR@.get().resolveCommand("island") != null) {
      @CMGR@.get().handleCommand(pr, "island");
      return;
    }
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not run /island after a switch: " + t); }
  try {
    if (st.getComponent(ref, @TP@.getComponentType()) != null) return;
    @WLD@ target = @UNI@.get().getDefaultWorld();
    if (target == null) return;
    @TRF@ where = target.getWorldConfig().getSpawnProvider().getSpawnPoint(target, pr.getUuid());
    if (where == null) return;
    ((@CA@) st).addComponent(ref, @TP@.getComponentType(), @TP@.createForPlayer(target, where));
    pr.sendMessage(@MSG@.raw("[Profiles] SkyyIslands is not installed - you were sent to the world spawn."));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("fallback teleport after a switch failed: " + t); }
}""")
M(sw, r"""
public static boolean rollback(@ST@ st, @REF@ ref, @PLA@ player, java.util.UUID u, org.bson.BsonDocument cur, String why) {
  try {
    @PKG@.ProfInv.clearAll(st, ref);
    int[] r = @PKG@.ProfInv.loadInto(st, ref, player, cur, u, true);
    clearMarker(u);
    @PKG@.ProfStore.log("ROLLBACK " + u + " (" + why + ") restored=" + r[0] + " moved=" + r[1] + " skipped=" + r[2]);
    return true;
  } catch (Throwable t) {
    @PKG@.ProfCfg.warn("ROLLBACK FAILED for " + u + " (" + why + "): " + t + " - snapshots stay in inventories/, marker set to failed");
    failMarker(u);
    @PKG@.ProfStore.log("ROLLBACK-FAILED " + u + " (" + why + ") " + t);
    return false;
  }
}""")
M(sw, r"""
public static String switchLocked(@ST@ st, @REF@ ref, @PR@ pr, @PLA@ player, java.util.UUID u, java.util.Properties p, String from, String to) {
  String fromKey = @PKG@.ProfStore.keyFor(u, from);
  String toKey = @PKG@.ProfStore.keyFor(u, to);
  String toName = p.getProperty("p." + to + ".name", "Profile " + to);
  String toCls = p.getProperty("p." + to + ".class", "");
  boolean toHad = "1".equals(p.getProperty("p." + to + ".inv"));
  org.bson.BsonDocument target = null;
  if (toHad) {
    target = @PKG@.ProfInv.read(toKey);
    if (target == null) return "The saved inventory of " + toName + " is missing or unreadable - nothing was changed. Ask an admin (inventories/" + toKey + ".json).";
  } else {
    target = @PKG@.ProfInv.empty(u, to, toKey);
  }
  org.bson.BsonDocument cur = @PKG@.ProfInv.capture(st, ref, u, from, fromKey);
  int saved = @PKG@.ProfInv.slotCount(cur);
  if (!@PKG@.ProfInv.write(fromKey, cur)) return "Could not save your current inventory - nothing was changed.";
  if (!writeMarker(u, from, to, fromKey, toKey, toHad)) return "Could not start the switch - nothing was changed.";
  @PKG@.ProfStore.PENDING.remove(u);
  int[] res = null;
  try {
    if (!@PKG@.ProfInv.clearAll(st, ref)) {
      boolean ok0 = rollback(st, ref, player, u, cur, "clear failed");
      return ok0 ? "Could not empty your inventory - the switch was cancelled and your items are back." : "The switch failed. Please relog - an admin may need to restore your items (server log).";
    }
    res = @PKG@.ProfInv.loadInto(st, ref, player, target, u, false);
  } catch (Throwable t) {
    @PKG@.ProfCfg.warn("switch " + u + " " + from + "->" + to + " failed while swapping items: " + t);
    boolean ok1 = rollback(st, ref, player, u, cur, "swap failed " + t);
    return ok1 ? "The switch failed and was cancelled - your items are back." : "The switch failed. Please relog - an admin may need to restore your items (server log).";
  }
  if (!@PKG@.ProfStore.setActive(u, from, to)) {
    boolean ok2 = rollback(st, ref, player, u, cur, "players file not saved");
    return ok2 ? "Could not save the profile change - the switch was cancelled and your items are back." : "The switch failed. Please relog - an admin may need to restore your items (server log).";
  }
  @PKG@.ClearLater.arm(u);
  @PKG@.ProfStore.publish(u);
  @PKG@.ProfStore.log("SWITCH " + u + " " + pr.getUsername() + " " + from + "->" + to + " saved=" + saved + " loaded=" + res[0] + " moved=" + res[1] + " skipped=" + res[2]);
  closeOurPage(ref, st, player);
  pr.sendMessage(@MSG@.raw("[Profiles] Switched to " + toName + (toCls.length() > 0 ? " (" + toCls + ")" : "") + ". Your other inventory is saved with its profile.").color("#8fe39a"));
  if (res[1] > 0) pr.sendMessage(@MSG@.raw("[Profiles] " + res[1] + " stack(s) could not go back to their old slot and were added to your inventory (dropped at your feet if it was full).").color("#ffc800"));
  travel(st, ref, pr);
  return null;
}""")
M(sw, r"""
public static String switchTo(@ST@ st, @REF@ ref, @PR@ pr, @PLA@ player, String to) {
  java.util.UUID u = pr.getUuid();
  if (@PKG@.ProfStore.BROKEN.containsKey(u)) return "Your profile file could not be read - ask an admin (server log).";
  java.util.Properties p = @PKG@.ProfStore.load(u);
  String from = @PKG@.ProfStore.activeOf(p);
  if (from == null) return "Create your first profile first (/profiles create).";
  if (to == null || !@PKG@.ProfStore.exists(p, to)) return "You have no such profile. /profiles list shows yours.";
  if (to.equals(from)) return "You are already on that profile.";
  String why = refuse(st, ref, player);
  if (why != null) return why;
  if (@PKG@.ProfStore.BUSY.putIfAbsent(u, Boolean.TRUE) != null) return "A profile switch is already running.";
  String mb = markerBlock(u);
  if (mb != null) { @PKG@.ProfStore.BUSY.remove(u); return mb; }
  String r = null;
  @PKG@.ProfStore.busy(u, true);
  try { r = switchLocked(st, ref, pr, player, u, p, from, to); }
  catch (Throwable t) { @PKG@.ProfCfg.warn("switch failed for " + u + ": " + t); r = "The switch failed: " + t.getMessage(); }
  @PKG@.ProfStore.busy(u, false);
  @PKG@.ProfStore.BUSY.remove(u);
  return r;
}""")
M(sw, r"""
public static String createFirst(@PR@ pr, String cls, String name) {
  java.util.UUID u = pr.getUuid();
  if (@PKG@.ProfStore.BROKEN.containsKey(u)) return "Your profile file could not be read - ask an admin (server log).";
  java.util.Properties p = @PKG@.ProfStore.load(u);
  if (@PKG@.ProfStore.activeOf(p) != null) return "You already have a profile.";
  if (name == null || name.length() == 0 || @PKG@.ProfNames.used(p, name)) name = @PKG@.ProfNames.pick(p, null);
  if (!@PKG@.ProfStore.create(u, "1", name, cls, true)) return "Could not save your profile - try again or ask an admin.";
  @PKG@.ProfStore.publish(u);
  @PKG@.ProfStore.log("CREATE " + u + " " + pr.getUsername() + " profile 1 " + name + " " + cls + " (first profile = existing data)");
  pr.sendMessage(@MSG@.raw("[Profiles] Profile " + name + " created - you are " + @PKG@.ProfRoster.article(cls) + " for good on this profile. Everything you already had belongs to it. /profiles manages your profiles.").color("#8fe39a"));
  // 0.1.2: the class kit - after create + publish returned, no SkyyProfiles monitor held; profile 1 = key uuid.toString()
  @PKG@.ProfKit.kitNew(u, @PKG@.ProfStore.keyFor(u, "1"), cls);
  return null;
}""")
M(sw, r"""
public static String createAndSwitch(@ST@ st, @REF@ ref, @PR@ pr, @PLA@ player, String cls, String name) {
  java.util.UUID u = pr.getUuid();
  if (@PKG@.ProfStore.BROKEN.containsKey(u)) return "Your profile file could not be read - ask an admin (server log).";
  java.util.Properties p = @PKG@.ProfStore.load(u);
  if (@PKG@.ProfStore.activeOf(p) == null) return createFirst(pr, cls, name);
  if (@PKG@.ProfStore.count(p) >= @PKG@.ProfCfg.MAX_PROFILES) return "All " + @PKG@.ProfCfg.MAX_PROFILES + " profile slots are used.";
  if (@PKG@.ProfStore.BUSY.containsKey(u)) return "A profile switch is already running.";
  String mb = markerBlock(u);
  if (mb != null) return mb;
  String why = refuse(st, ref, player);
  if (why != null) return why;
  String id = @PKG@.ProfStore.nextId(p);
  if (id == null) return "No free profile id.";
  if (name == null || name.length() == 0 || @PKG@.ProfNames.used(p, name)) name = @PKG@.ProfNames.pick(p, null);
  if (!@PKG@.ProfStore.create(u, id, name, cls, false)) return "Could not save the new profile - nothing was changed.";
  @PKG@.ProfStore.publish(u);
  @PKG@.ProfStore.log("CREATE " + u + " " + pr.getUsername() + " profile " + id + " " + name + " " + cls);
  // 0.1.2: the class kit - BEFORE the switch, so a failed switch still leaves it pending for that profile (SkyyClasses delivers only
  // to the ACTIVE key, after profile:busy clears and 31 s after the epoch change); no SkyyProfiles monitor is held here
  @PKG@.ProfKit.kitNew(u, @PKG@.ProfStore.keyFor(u, id), cls);
  String err = switchTo(st, ref, pr, player, id);
  if (err != null) return "Profile " + name + " was created but the switch did not happen - " + err + " Use Switch when you are ready.";
  return null;
}""")
# off the world thread at join: which snapshot must become the live inventory? null = nothing to do (or left for an admin)
M(sw, r"""
public static Object[] recoverPlan(java.util.UUID u) {
  if (@PKG@.ProfStore.PENDING.containsKey(u)) return null;
  java.util.Properties m = readMarker(u);
  if (m == null) return null;
  String stage = m.getProperty("stage", "");
  if (stage.equals("done")) { clearMarker(u); return null; }
  String from = m.getProperty("from");
  String to = m.getProperty("to");
  if (stage.equals("failed")) {
    String note = m.getProperty("note", "");
    @PKG@.ProfCfg.warn("player " + u + " has a FAILED switch marker (" + from + "->" + to + ") - " + (note.length() > 0 ? note : "restore by hand from inventories/, then delete switching/" + u + ".properties") + " (profile switching is refused until then)");
    return new Object[] { null, null, "failed" };
  }
  String act = @PKG@.ProfStore.activeId(u);
  if (act == null || from == null || to == null) { @PKG@.ProfCfg.warn("switch marker for " + u + " cannot be matched (active " + act + ") - left alone"); return null; }
  if (act.equals(from)) {
    org.bson.BsonDocument d = @PKG@.ProfInv.read(m.getProperty("fromKey", ""));
    if (d == null) { @PKG@.ProfCfg.warn("switch marker for " + u + ": snapshot " + m.getProperty("fromKey") + " is missing - left alone"); return null; }
    return new Object[] { d, from, "rolled back" };
  }
  if (act.equals(to)) {
    org.bson.BsonDocument d2 = null;
    if ("1".equals(m.getProperty("toHadFile"))) {
      d2 = @PKG@.ProfInv.read(m.getProperty("toKey", ""));
      if (d2 == null) { @PKG@.ProfCfg.warn("switch marker for " + u + ": snapshot " + m.getProperty("toKey") + " is missing - left alone"); return null; }
    } else {
      d2 = @PKG@.ProfInv.empty(u, to, m.getProperty("toKey", ""));
    }
    return new Object[] { d2, to, "finished" };
  }
  @PKG@.ProfCfg.warn("switch marker for " + u + " names " + from + "->" + to + " but the active profile is " + act + " - left alone");
  return null;
}""")
# loadInto in rollbackMode like rollback(): a slot clearAll could not empty that already holds the identical saved stack counts as
# restored (never given a second time). clearAll not fully verified -> the marker becomes stage=failed with a note (an admin checks the
# stuck slots for duplicates; switching is refused until the marker is deleted) instead of reporting a clean recovery.
M(sw, r"""
public static boolean recoverApply(@ST@ st, @REF@ ref, @PR@ pr, @PLA@ player, org.bson.BsonDocument doc, String which, String how) {
  java.util.UUID u = pr.getUuid();
  boolean ok = @PKG@.ProfInv.clearAll(st, ref);
  int[] r = @PKG@.ProfInv.loadInto(st, ref, player, doc, u, true);
  java.util.Properties p = @PKG@.ProfStore.load(u);
  String name = p.getProperty("p." + which + ".name", "Profile " + which);
  if (!ok) {
    failMarkerNote(u, "crash recovery (" + how + " to profile " + which + ") loaded that snapshot but could not empty every slot first - check the slots named in the server log (could not empty ...) for duplicates, then delete switching/" + u + ".properties");
    @PKG@.ProfCfg.warn("crash recovery for " + u + " (" + how + ", profile " + which + ") could not empty every slot first - snapshot loaded without re-giving identical stuck stacks, marker set to failed for an admin");
    @PKG@.ProfStore.publish(u);
    @PKG@.ProfStore.log("RECOVER-INCOMPLETE " + u + " " + pr.getUsername() + " profile " + which + " " + how + " loaded=" + r[0] + " moved=" + r[1] + " skipped=" + r[2] + " (some slots could not be emptied - marker left as failed for an admin)");
    @PKG@.ProfStore.notice(pr, @MSG@.raw("[Profiles] Your last profile switch was interrupted by a server stop and could not be fully repaired - you are on " + name + " but some slots could not be emptied first. Profile switching is paused until an admin checks it. Please tell an admin.").color("#ff9d6b"));
    return false;
  }
  @PKG@.ClearLater.arm(u);
  @PKG@.ProfStore.publish(u);
  @PKG@.ProfStore.log("RECOVER " + u + " " + pr.getUsername() + " profile " + which + " " + how + " loaded=" + r[0] + " moved=" + r[1] + " skipped=" + r[2]);
  @PKG@.ProfStore.notice(pr, @MSG@.raw("[Profiles] Your last profile switch was interrupted by a server stop. It was " + how + " - you are on " + name + " with its saved inventory.").color("#ffc800"));
  return true;
}""")

# ================= ProfilePage: Profiles list (view 0) + Create Profile (view 1) =================
# 0.1.2: button labels 14 pt (were 12) - the page is 1000 x 900 now
BTN = ("Style: TextButtonStyle(Default: (Background: #5a4420, LabelStyle: (FontSize: 14, TextColor: #ffe9c9, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)), "
       "Hovered: (Background: #8a6a30, LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)), "
       "Pressed: (Background: #3a2a10, LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)));")
BTN_GO = ("Style: TextButtonStyle(Default: (Background: #2f6a3a, LabelStyle: (FontSize: 14, TextColor: #e9ffe9, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)), "
          "Hovered: (Background: #3f8a4a, LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)), "
          "Pressed: (Background: #1f4a2a, LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: true, HorizontalAlignment: Center, VerticalAlignment: Center)));")
def ui(src):
    """Page source: button styles, root size + padding, and the 0.1.2 layout constants (__C<key>__ from CL, __L<key>__ from LL)."""
    out = (src.replace("__BTN__", BTN.replace('"', '\\"')).replace("__BTNGO__", BTN_GO.replace('"', '\\"'))
              .replace("__W__", str(PAGE_W)).replace("__H__", str(PAGE_H)).replace("__PADH__", str(PAD_H)).replace("__PADV__", str(PAD_V)))
    for k in sorted(CL, key=len, reverse=True):
        out = out.replace("__C" + k + "__", str(CL[k]))
    for k in sorted(LL, key=len, reverse=True):
        out = out.replace("__L" + k + "__", str(LL[k]))
    left = re.findall(r"__[A-Z0-9]+__", out)
    need(not left, "unreplaced page tokens: %s" % left)
    return out
F(page, "public int view;")
F(page, "public boolean first;")
F(page, "public String pending;")
F(page, "public String pickName;")
F(page, "public String pickClass;")
F(page, "public String info;")
F(page, "public boolean reminded;")
F(page, "public static final int MAX_CARDS = %d;" % MAX_CARDS)      # 0.1.2: cards drawn + pfcls<i> handled (0.1.1: 6)
M(page, r"""
public static String safe(String t) {
  if (t == null) return "";
  return t.replace(':', ' ').replace(';', ' ').replace(',', ' ').replace('{', '(').replace('}', ')').replace('"', ' ').replace('\'', ' ').replace('\\', ' ');
}""")
# first profile: pre-select the class the player already has in SkyyClasses 0.1.x (bridge class:<uuid>, else class:fn:get)
M(page, r"""
public void prepareCreate() {
  java.util.UUID u = this.playerRef.getUuid();
  java.util.Properties p = @PKG@.ProfStore.load(u);
  this.pickName = @PKG@.ProfNames.pick(p, null);
  this.pickClass = null;
  if (!this.first) return;
  String had = null;
  try {
    Object o = @PKG@.ProfCfg.bridge().get("class:" + u.toString());
    if (o instanceof String) had = (String) o;
    if (had == null) {
      Object f = @PKG@.ProfCfg.bridge().get("class:fn:get");
      if (f instanceof java.util.function.Function) {
        Object r = ((java.util.function.Function) f).apply(u);
        if (r instanceof String) had = (String) r;
      }
    }
  } catch (Throwable t) { }
  String[] c = @PKG@.ProfRoster.entry(had);
  if (c != null && "1".equals(c[6])) {
    this.pickClass = c[0];
    this.info = "Your current class " + c[0] + " is pre-selected.";
  }
}""")
M(page, ui(r"""
public void buildList(@UCB@ b, @UEB@ ev, java.util.UUID u, java.util.Properties p) {
  String bs = "__BTN__";
  String go = "__BTNGO__";
  String act = @PKG@.ProfStore.activeOf(p);
  int n = @PKG@.ProfStore.count(p);
  int max = @PKG@.ProfCfg.MAX_PROFILES;
  b.appendInline((String) null, "Group #SkyyPf { Anchor: (Width: __W__, Height: __H__); Background: #0b1524(0.96); Padding: (Horizontal: __PADH__, Vertical: __PADV__); LayoutMode: Top; }");
  b.appendInline("#SkyyPf", "Group { Anchor: (Height: __LBAR__); Background: #d08a4a; }");
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __LTITLE__); Text: \"Profiles\"; Style: (FontSize: 20, RenderBold: true, TextColor: #ffe9c9, HorizontalAlignment: Center, VerticalAlignment: Center); }");
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __LSUB__); Text: \"Each profile is its own save - class, island, coins, skills, bags and inventory.\"; Style: (FontSize: 13, TextColor: #9fb8cc, HorizontalAlignment: Center, VerticalAlignment: Center); }");
  for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
    String id = String.valueOf(i);
    if (!@PKG@.ProfStore.exists(p, id)) continue;
    boolean on = id.equals(act);
    boolean pend = id.equals(this.pending);
    String name = p.getProperty("p." + id + ".name", "Profile " + id);
    String cls = p.getProperty("p." + id + ".class", "");
    String[] ce = @PKG@.ProfRoster.entry(cls);
    String color = ce == null ? "#c9d6e2" : ce[3];
    String bg = on ? "#173524(0.95)" : (pend ? "#3a2f1a(0.95)" : "#142030(0.9)");
    String cid = "#SkyyPfCard" + id;
    String tid = "#SkyyPfTxt" + id;
    String aid = "#SkyyPfAct" + id;
    b.appendInline("#SkyyPf", "Label { Anchor: (Height: __LGAP__); Text: \"\"; }");
    b.appendInline("#SkyyPf", "Group #SkyyPfCard" + id + " { Anchor: (Height: __LCARD__); LayoutMode: Left; Background: " + bg + "; }");
    b.appendInline(cid, "Label { Anchor: (Width: __LLPAD__, Height: __LCARD__); Text: \"\"; }");
    b.appendInline(cid, "Group { Anchor: (Width: __LICO__, Height: __LCARD__); ItemIcon { Anchor: (Width: __LICON__, Height: __LICON__, Left: __LICOL__, Top: __LICOT__); ItemId: \"" + safe(@PKG@.ProfRoster.iconOf(cls)) + "\"; } }");
    b.appendInline(cid, "Group #SkyyPfTxt" + id + " { Anchor: (Width: __LTEXT__, Height: __LCARD__); LayoutMode: Top; Padding: (Top: __LTPAD__); }");
    b.appendInline(tid, "Label { Anchor: (Height: __LT1__); Text: \"" + safe(name + (on ? " - ACTIVE" : "")) + "\"; Style: (FontSize: 17, RenderBold: true, TextColor: " + (on ? "#8fe39a" : "#ffe9c9") + ", VerticalAlignment: Center); }");
    String line2 = cls.length() == 0 ? "No class - an admin can set it" : cls + " - combat skill " + @PKG@.ProfRoster.skillOf(cls);
    b.appendInline(tid, "Label { Anchor: (Height: __LT2__); Text: \"" + safe(line2) + "\"; Style: (FontSize: 14, RenderBold: true, TextColor: " + color + ", VerticalAlignment: Center); }");
    String line3 = "Created " + @PKG@.ProfStore.fmtDate(@PKG@.ProfStore.num(p, "p." + id + ".created")) + " - "
      + (on ? "playing now" : "last played " + @PKG@.ProfStore.fmtAgo(@PKG@.ProfStore.num(p, "p." + id + ".lastPlayed")));
    b.appendInline(tid, "Label { Anchor: (Height: __LT3__); Text: \"" + safe(line3) + "\"; Style: (FontSize: 13, TextColor: #9fb8cc, VerticalAlignment: Center); }");
    b.appendInline(cid, "Group #SkyyPfAct" + id + " { Anchor: (Width: __LACT__, Height: __LCARD__); LayoutMode: Top; Padding: (Top: __LACTT__); }");
    if (on) {
      b.appendInline(aid, "Label { Anchor: (Width: __LBTNW__, Height: __LBTNH__); Text: \"Active\"; Style: (FontSize: 15, RenderBold: true, TextColor: #8fe39a, HorizontalAlignment: Center, VerticalAlignment: Center); }");
    } else {
      b.appendInline(aid, "TextButton #SkyyPfSw" + id + " { Anchor: (Width: __LBTNW__, Height: __LBTNH__); Text: \"Switch\"; " + (pend ? go : bs) + " }");
      ev.addEventBinding(@BT@.Activating, "#SkyyPfSw" + id, @EVD@.of("a", "pfsw" + id));
    }
  }
  if (n < max) {
    b.appendInline("#SkyyPf", "Label { Anchor: (Height: __LGAP__); Text: \"\"; }");
    b.appendInline("#SkyyPf", "Group #SkyyPfNewCard { Anchor: (Height: __LNEW__); LayoutMode: Left; Background: #0d1219(0.85); }");
    b.appendInline("#SkyyPfNewCard", "Label { Anchor: (Width: __LLPAD__, Height: __LNEW__); Text: \"\"; }");
    b.appendInline("#SkyyPfNewCard", "Group { Anchor: (Width: __LICO__, Height: __LNEW__); ItemIcon { Anchor: (Width: __LNEWICON__, Height: __LNEWICON__, Left: __LNEWICOL__, Top: __LNEWICOT__); ItemId: \"" + @PKG@.ProfRoster.NEW_ICON + "\"; } }");
    b.appendInline("#SkyyPfNewCard", "Label { Anchor: (Width: __LTEXT__, Height: __LNEW__); Text: \"" + safe("Empty slot - a new profile starts from zero with the class you pick (" + (max - n) + " free)") + "\"; Style: (FontSize: 14, TextColor: #c9d6e2, VerticalAlignment: Center, Wrap: true); }");
    b.appendInline("#SkyyPfNewCard", "Group #SkyyPfNewAct { Anchor: (Width: __LACT__, Height: __LNEW__); LayoutMode: Top; Padding: (Top: __LNEWACTT__); }");
    b.appendInline("#SkyyPfNewAct", "TextButton #SkyyPfNew { Anchor: (Width: __LBTNW__, Height: __LBTNH__); Text: \"Create new\"; " + go + " }");
    ev.addEventBinding(@BT@.Activating, "#SkyyPfNew", @EVD@.of("a", "pfnew"));
  }
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __LAFTER__); Text: \"\"; }");
  b.appendInline("#SkyyPf", "Label #SkyyPfInfo { Anchor: (Height: __LINFO__); Text: \"" + safe(this.info) + "\"; Style: (FontSize: 14, RenderBold: true, TextColor: #ffd27a, HorizontalAlignment: Center, VerticalAlignment: Center); }");
  if (this.pending != null && @PKG@.ProfStore.exists(p, this.pending) && !this.pending.equals(act)) {
    String pn = p.getProperty("p." + this.pending + ".name", "Profile " + this.pending);
    String pc = p.getProperty("p." + this.pending + ".class", "");
    String q = "Switch to " + pn + (pc.length() > 0 ? " (" + pc + ")" : "") + "? Your inventory is saved and you go to the island of that profile.";
    b.appendInline("#SkyyPf", "Group #SkyyPfConfirm { Anchor: (Height: __LCONF__); LayoutMode: Left; Padding: (Top: 3); }");
    b.appendInline("#SkyyPfConfirm", "Label { Anchor: (Width: __LCONFL__, Height: 34); Text: \"" + safe(q) + "\"; Style: (FontSize: 13, RenderBold: true, TextColor: #ffe9c9, VerticalAlignment: Center, Wrap: true); }");
    b.appendInline("#SkyyPfConfirm", "TextButton #SkyyPfYes { Anchor: (Width: __LCONFB__, Height: 34); Text: \"Confirm\"; " + go + " }");
    b.appendInline("#SkyyPfConfirm", "Label { Anchor: (Width: 10, Height: 34); Text: \"\"; }");
    b.appendInline("#SkyyPfConfirm", "TextButton #SkyyPfNo { Anchor: (Width: __LCONFB__, Height: 34); Text: \"Cancel\"; " + bs + " }");
    ev.addEventBinding(@BT@.Activating, "#SkyyPfYes", @EVD@.of("a", "pfyes"));
    ev.addEventBinding(@BT@.Activating, "#SkyyPfNo", @EVD@.of("a", "pfno"));
  } else {
    String used = n + " of " + max + " profile slots used.";
    if (n > max) used = n + " profiles - this server now allows " + max + ". You keep them all but cannot create more.";
    String foot = used + " Switching saves your inventory and takes you to the island of the other profile. Not while in combat.";
    b.appendInline("#SkyyPf", "Label #SkyyPfFoot { Anchor: (Height: __LFOOT__); Text: \"" + safe(foot) + "\"; Style: (FontSize: 13, TextColor: #8fa4b8, HorizontalAlignment: Center, VerticalAlignment: Center, Wrap: true); }");
  }
}"""))
M(page, ui(r"""
public void buildCreate(@UCB@ b, @UEB@ ev, java.util.UUID u, java.util.Properties p) {
  String bs = "__BTN__";
  String go = "__BTNGO__";
  String newId = this.first ? "1" : @PKG@.ProfStore.nextId(p);
  if (newId == null) newId = "?";
  b.appendInline((String) null, "Group #SkyyPf { Anchor: (Width: __W__, Height: __H__); Background: #0b1524(0.96); Padding: (Horizontal: __PADH__, Vertical: __PADV__); LayoutMode: Top; }");
  b.appendInline("#SkyyPf", "Group { Anchor: (Height: __CBAR__); Background: #d08a4a; }");
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __CTITLE__); Text: \"" + (this.first ? "Create your first profile" : "Create a new profile") + "\"; Style: (FontSize: 20, RenderBold: true, TextColor: #ffe9c9, HorizontalAlignment: Center, VerticalAlignment: Center); }");
  b.appendInline("#SkyyPf", "Group #SkyyPfNameRow { Anchor: (Height: __CNAME__); LayoutMode: Left; Padding: (Top: 2); }");
  b.appendInline("#SkyyPfNameRow", "Label { Anchor: (Width: __CNAMEL__, Height: __CNAMEBH__); Text: \"" + safe("Profile " + newId + " will be called " + this.pickName) + "\"; Style: (FontSize: 15, RenderBold: true, TextColor: #ffe9c9, VerticalAlignment: Center); }");
  b.appendInline("#SkyyPfNameRow", "TextButton #SkyyPfName { Anchor: (Width: __CNAMEB__, Height: __CNAMEBH__); Text: \"Other name\"; " + bs + " }");
  ev.addEventBinding(@BT@.Activating, "#SkyyPfName", @EVD@.of("a", "pfname"));
  boolean kits = @PKG@.ProfKit.available();
  String sub = "";
  if (this.first) {
    sub = kits ? "Pick your class. It is locked to this profile forever. You get your class kit - its basic weapon - right after creating it. Everything you already have stays on this first profile."
               : "Pick your class. It is locked to this profile forever - like creating a world. Everything you already have stays on this first profile.";
  } else {
    sub = kits ? "Pick your class. It is locked to this profile forever. You get your class kit - its basic weapon - right after creating it. The profile starts from zero with its own island and coins."
               : "Pick a class. It is locked to this profile forever. The new profile starts from zero - empty inventory and its own island and coins and skills.";
  }
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __CSUB__); Text: \"" + safe(sub) + "\"; Style: (FontSize: 12, TextColor: #9fb8cc, HorizontalAlignment: Center, VerticalAlignment: Center, Wrap: true); }");
  java.util.ArrayList r = @PKG@.ProfRoster.roster();
  int shown = r.size() < MAX_CARDS ? r.size() : MAX_CARDS;
  boolean tight = shown > 7;
  int ch = tight ? __CCARD8__ : __CCARD__;
  int gap = tight ? __CGAP8__ : __CGAP__;
  int tp = tight ? __CTPAD8__ : __CTPAD__;
  int t1 = tight ? __CT18__ : __CT1__;
  int t4 = tight ? __CT48__ : __CT4__;
  int it = (ch - __CICON__) / 2;
  int at = (ch - __CBTNH__) / 2;
  for (int i = 0; i < shown; i++) {
    String[] c = (String[]) r.get(i);
    boolean on = "1".equals(c[6]);
    boolean sel = this.pickClass != null && c[0].equalsIgnoreCase(this.pickClass);
    String bg = sel ? "#173524(0.95)" : (on ? "#142030(0.9)" : "#0d1219(0.85)");
    String role = c.length > 7 && c[7] != null ? c[7] : "";
    String cid = "#SkyyPfCls" + i;
    String iid = "#SkyyPfIco" + i;
    String tid = "#SkyyPfCTx" + i;
    String aid = "#SkyyPfCAct" + i;
    b.appendInline("#SkyyPf", "Label { Anchor: (Height: " + gap + "); Text: \"\"; }");
    b.appendInline("#SkyyPf", "Group #SkyyPfCls" + i + " { Anchor: (Height: " + ch + "); LayoutMode: Left; Background: " + bg + "; }");
    b.appendInline(cid, "Label { Anchor: (Width: __CLPAD__, Height: " + ch + "); Text: \"\"; }");
    b.appendInline(cid, "Group #SkyyPfIco" + i + " { Anchor: (Width: __CICOS__, Height: " + ch + "); LayoutMode: Left; }");
    String[] ic = c[5].split(",");
    for (int k = 0; k < ic.length && k < 3; k++) {
      b.appendInline(iid, "Group { Anchor: (Width: __CICO__, Height: " + ch + "); ItemIcon { Anchor: (Width: __CICON__, Height: __CICON__, Left: __CICOL__, Top: " + it + "); ItemId: \"" + safe(ic[k]) + "\"; } }");
    }
    b.appendInline(cid, "Group #SkyyPfCTx" + i + " { Anchor: (Width: __CTEXT__, Height: " + ch + "); LayoutMode: Top; Padding: (Top: " + tp + "); }");
    String title = c[0] + (sel ? " - selected" : (on ? "" : " - coming later"));
    b.appendInline(tid, "Label { Anchor: (Height: " + t1 + "); Text: \"" + safe(title) + "\"; Style: (FontSize: 16, RenderBold: true, TextColor: " + (on ? c[3] : "#5f6b78") + ", VerticalAlignment: Center); }");
    b.appendInline(tid, "Label { Anchor: (Height: __CT2__); Text: \"" + safe("Combat skill " + c[1] + " - " + c[4]) + "\"; Style: (FontSize: 12, RenderBold: true, TextColor: " + (on ? "#9fd8a2" : "#5f6b78") + ", VerticalAlignment: Center); }");
    b.appendInline(tid, "Label { Anchor: (Height: __CT3__); Text: \"" + safe(role) + "\"; Style: (FontSize: 12, TextColor: " + (on ? c[3] : "#5f6b78") + ", VerticalAlignment: Center); }");
    b.appendInline(tid, "Label { Anchor: (Height: " + t4 + "); Text: \"" + safe(c[2]) + "\"; Style: (FontSize: 11, TextColor: " + (on ? "#c9d6e2" : "#5f6b78") + ", VerticalAlignment: Center, Wrap: true); }");
    b.appendInline(cid, "Group #SkyyPfCAct" + i + " { Anchor: (Width: __CACT__, Height: " + ch + "); LayoutMode: Top; Padding: (Top: " + at + "); }");
    if (!on) {
      b.appendInline(aid, "Label { Anchor: (Width: __CBTNW__, Height: __CBTNH__); Text: \"Coming later\"; Style: (FontSize: 14, RenderBold: true, TextColor: #5f6b78, HorizontalAlignment: Center, VerticalAlignment: Center); }");
    } else if (sel) {
      b.appendInline(aid, "Label { Anchor: (Width: __CBTNW__, Height: __CBTNH__); Text: \"Selected\"; Style: (FontSize: 15, RenderBold: true, TextColor: #8fe39a, HorizontalAlignment: Center, VerticalAlignment: Center); }");
    } else {
      b.appendInline(aid, "TextButton #SkyyPfPick" + i + " { Anchor: (Width: __CBTNW__, Height: __CBTNH__); Text: \"Select\"; " + bs + " }");
      ev.addEventBinding(@BT@.Activating, "#SkyyPfPick" + i, @EVD@.of("a", "pfcls" + i));
    }
  }
  b.appendInline("#SkyyPf", "Label { Anchor: (Height: __CAFTER__); Text: \"\"; }");
  String q = this.pickClass == null ? "Select a class to continue."
    : "Create " + this.pickName + " as " + @PKG@.ProfRoster.article(this.pickClass) + "? The class can never be changed.";
  b.appendInline("#SkyyPf", "Group #SkyyPfMakeRow { Anchor: (Height: __CMAKE__); LayoutMode: Left; Padding: (Top: 3); }");
  b.appendInline("#SkyyPfMakeRow", "Label { Anchor: (Width: __CMAKEL__, Height: __CBTNH__); Text: \"" + safe(q) + "\"; Style: (FontSize: 14, RenderBold: true, TextColor: #ffe9c9, VerticalAlignment: Center, Wrap: true); }");
  if (this.pickClass != null) {
    b.appendInline("#SkyyPfMakeRow", "TextButton #SkyyPfMake { Anchor: (Width: __CMAKEB__, Height: __CBTNH__); Text: \"Create profile\"; " + go + " }");
    ev.addEventBinding(@BT@.Activating, "#SkyyPfMake", @EVD@.of("a", "pfmake"));
  } else {
    b.appendInline("#SkyyPfMakeRow", "Label { Anchor: (Width: __CMAKEB__, Height: __CBTNH__); Text: \"\"; }");
  }
  b.appendInline("#SkyyPfMakeRow", "Label { Anchor: (Width: 10, Height: __CBTNH__); Text: \"\"; }");
  b.appendInline("#SkyyPfMakeRow", "TextButton #SkyyPfBack { Anchor: (Width: __CBACKB__, Height: __CBTNH__); Text: \"" + (this.first ? "Later" : "Back") + "\"; " + bs + " }");
  ev.addEventBinding(@BT@.Activating, "#SkyyPfBack", @EVD@.of("a", "pfback"));
  b.appendInline("#SkyyPf", "Label #SkyyPfInfo { Anchor: (Height: __CINFO__); Text: \"" + safe(this.info) + "\"; Style: (FontSize: 14, RenderBold: true, TextColor: #ffd27a, HorizontalAlignment: Center, VerticalAlignment: Center); }");
}"""))
M(page, r"""
public void build(@REF@ ref, @UCB@ b, @UEB@ ev, @ST@ st) {
  java.util.UUID u = this.playerRef.getUuid();
  java.util.Properties p = @PKG@.ProfStore.load(u);
  if (@PKG@.ProfStore.activeOf(p) == null) { this.first = true; this.view = 1; }
  if (this.view == 1 && (this.pickName == null || this.pickName.length() == 0)) prepareCreate();
  if (this.view == 1) buildCreate(b, ev, u, p); else buildList(b, ev, u, p);
}""")
M(page, r"""
public void closeSelf(@REF@ ref, @ST@ st) {
  try {
    @PLA@ player = (@PLA@) st.getComponent(ref, @PLA@.getComponentType());
    if (player != null) player.getPageManager().setPage(ref, st, @PGE@.None);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not close the profiles page: " + t); }
}""")
M(page, r"""
public void remindFirst() {
  try {
    if (this.reminded || @PKG@.ProfStore.activeId(this.playerRef.getUuid()) != null) return;
    this.reminded = true;
    this.playerRef.sendMessage(@MSG@.raw("[Profiles] You have no profile yet. Type /profiles to create one - you pick your class (" + @PKG@.ProfRoster.choiceText() + ") and it is locked to that profile.").color("#ffc800"));
  } catch (Throwable t) { }
}""")
M(page, r"""
public void handleDataEvent(@REF@ ref, @ST@ st, String data) {
  try {
    if (data == null) return;
    java.util.UUID u = this.playerRef.getUuid();
    java.util.Properties p = @PKG@.ProfStore.load(u);
    for (int i = 1; i <= @PKG@.ProfCfg.MAX_ID; i++) {
      if (data.indexOf("pfsw" + i + "\"") < 0) continue;
      String id = String.valueOf(i);
      this.pending = @PKG@.ProfStore.exists(p, id) ? id : null;
      this.info = "";
      rebuild();
      return;
    }
    if (data.indexOf("pfyes\"") >= 0) {
      String to = this.pending;
      this.pending = null;
      if (to == null) { rebuild(); return; }
      @PLA@ player = (@PLA@) st.getComponent(ref, @PLA@.getComponentType());
      if (player == null) return;
      String err = @PKG@.ProfSwitch.switchTo(st, ref, this.playerRef, player, to);
      if (err != null) { this.info = err; rebuild(); }
      return;
    }
    if (data.indexOf("pfno\"") >= 0) { this.pending = null; this.info = ""; rebuild(); return; }
    if (data.indexOf("pfnew\"") >= 0) {
      if (@PKG@.ProfStore.count(p) >= @PKG@.ProfCfg.MAX_PROFILES) { this.info = "All " + @PKG@.ProfCfg.MAX_PROFILES + " profile slots are used."; rebuild(); return; }
      this.view = 1;
      this.pending = null;
      this.info = "";
      prepareCreate();
      rebuild();
      return;
    }
    java.util.ArrayList r = @PKG@.ProfRoster.roster();
    for (int i = 0; i < r.size() && i < MAX_CARDS; i++) {
      if (data.indexOf("pfcls" + i + "\"") < 0) continue;
      String[] c = (String[]) r.get(i);
      if ("1".equals(c[6])) { this.pickClass = c[0]; this.info = ""; }
      else this.info = c[0] + " is coming later.";
      rebuild();
      return;
    }
    if (data.indexOf("pfname\"") >= 0) { this.pickName = @PKG@.ProfNames.pick(p, this.pickName); rebuild(); return; }
    if (data.indexOf("pfmake\"") >= 0) {
      // 0.1.2 (review fix): ONE create per selection - take the pick BEFORE any create work, put it back only where the create form shows
      // again with nothing created; a first-profile form whose player already has a profile goes to the list instead of creating another
      String pick = this.pickClass;
      this.pickClass = null;
      if (this.first && @PKG@.ProfStore.activeOf(p) != null) { this.view = 0; this.first = false; this.info = "Your first profile already exists - Create new makes another one."; rebuild(); return; }
      if (pick == null) { this.info = "Select a class first."; rebuild(); return; }
      String[] ce = @PKG@.ProfRoster.entry(pick);
      if (ce == null || !"1".equals(ce[6])) { this.info = pick + " is not available."; rebuild(); return; }
      @PLA@ player = (@PLA@) st.getComponent(ref, @PLA@.getComponentType());
      if (player == null) { this.pickClass = pick; return; }
      if (@PKG@.ProfStore.activeOf(p) == null) {
        String e1 = @PKG@.ProfSwitch.createFirst(this.playerRef, ce[0], this.pickName);
        if (e1 == null) { closeSelf(ref, st); return; }
        this.info = e1;
        this.pickClass = pick;
        rebuild();
        return;
      }
      int before = @PKG@.ProfStore.count(p);
      String e2 = @PKG@.ProfSwitch.createAndSwitch(st, ref, this.playerRef, player, ce[0], this.pickName);
      if (e2 == null) return;
      this.info = e2;
      if (@PKG@.ProfStore.count(@PKG@.ProfStore.load(u)) > before) { this.view = 0; this.first = false; }
      else this.pickClass = pick;
      rebuild();
      return;
    }
    if (data.indexOf("pfback\"") >= 0) {
      if (this.first || @PKG@.ProfStore.activeOf(p) == null) { closeSelf(ref, st); remindFirst(); return; }
      this.view = 0;
      this.info = "";
      rebuild();
      return;
    }
  } catch (Throwable t) { @PKG@.ProfCfg.warn("profiles page event failed: " + t); }
}""")
M(page, r"""
public void onDismiss(@REF@ ref, @ST@ st) {
  if (this.first) remindFirst();
}""")
C(page, r"""
public ProfilePage(@PR@ pr, int view, boolean first) {
  super(pr, @LIFE@.CanDismiss);
  this.view = view;
  this.first = first;
  this.pending = null;
  this.info = "";
  this.pickName = "";
  this.pickClass = null;
  this.reminded = false;
  if (view == 1) prepareCreate();
}""")
M(page, r"""
public static String open(@ST@ st, @REF@ ref, @PR@ pr, int view) {
  @PLA@ player = (@PLA@) st.getComponent(ref, @PLA@.getComponentType());
  if (player == null) return "Could not open the page.";
  java.util.UUID u = pr.getUuid();
  if (@PKG@.ProfStore.BROKEN.containsKey(u)) return "Your profile file could not be read - ask an admin (server log).";
  java.util.Properties p = @PKG@.ProfStore.load(u);
  boolean first = @PKG@.ProfStore.activeOf(p) == null;
  int v = first ? 1 : view;
  if (v == 1 && !first && @PKG@.ProfStore.count(p) >= @PKG@.ProfCfg.MAX_PROFILES) return "All " + @PKG@.ProfCfg.MAX_PROFILES + " profile slots are used.";
  player.getPageManager().openCustomPage(ref, st, new @PKG@.ProfilePage(pr, v, first));
  return null;
}""")

# ================= OpenTask: first-join Create Profile page (SkyyHud/SkyyClasses pattern: delay, world-thread hop, WorldMap channel gate) =================
# Cross-mod timeline pass (2026-09-23): every poll hops to the player's CURRENT world and re-checks on that thread that the player is
# still there (a world switch between the hop and the run left us on the wrong world thread); the page only opens after the player has
# been in the SAME world for 2 polls in a row (calm resets on a world change); while the player stands in a SkyyIslands island world
# during the first ISLAND_WAIT_MS we wait (SkyyIslands routes a login inside an island world to the hub 1.5 s after the first
# PlayerReadyEvent - a page opened just before that cross-world teleport is lost on the client and this login would show none);
# a custom page that is already open is never replaced (the player opened /profiles themselves -> done; any other page, e.g. the
# SkyyMenu page -> wait until it is closed); never opened over the respawn screen (DeathComponent). Gives up quietly after 240 polls
# (2 min) - the welcome chat line already points to /profiles and the page comes back at the next login.
opn.addInterface(pool.get("java.lang.Runnable"))
F(opn, "public static final long ISLAND_WAIT_MS = 8000L;")
F(opn, "public @PR@ pr;")
F(opn, "public boolean onWorld;")
F(opn, "public int calm;")
F(opn, "public int tries;")
F(opn, "public java.util.UUID hopWorld;")
F(opn, "public java.util.UUID lastWorld;")
F(opn, "public long born;")
C(opn, "public OpenTask(@PR@ pr) { this.pr = pr; this.onWorld = false; this.calm = 0; this.tries = 0; this.hopWorld = null; this.lastWorld = null; this.born = System.currentTimeMillis(); }")
M(opn, r"""
public void later(long ms) {
  this.onWorld = false;
  @HSV@.SCHEDULED_EXECUTOR.schedule(this, ms, java.util.concurrent.TimeUnit.MILLISECONDS);
}""")
M(opn, r"""
public void again(boolean unsettled) {
  if (unsettled) this.calm = 0;
  if (++this.tries < 240) later(500L);
}""")
M(opn, r"""
public void run() {
  try {
    if (pr == null || !pr.isValid()) return;
    if (!this.onWorld) {
      java.util.UUID wu = pr.getWorldUuid();
      if (wu == null) { again(true); return; }
      @WLD@ w = @UNI@.get().getWorld(wu);
      if (w == null) { again(true); return; }
      this.onWorld = true;
      this.hopWorld = wu;
      w.execute(this);
      return;
    }
    java.util.UUID u = pr.getUuid();
    if (@PKG@.ProfStore.activeId(u) != null) return;
    java.util.UUID now = pr.getWorldUuid();
    if (now == null || !now.equals(this.hopWorld)) { again(true); return; }
    if (this.lastWorld == null || !this.lastWorld.equals(now)) { this.lastWorld = now; this.calm = 0; }
    @WLD@ here = @UNI@.get().getWorld(now);
    String wn = here == null ? null : here.getName();
    if (wn != null && wn.startsWith("skyy-island-") && System.currentTimeMillis() - this.born < ISLAND_WAIT_MS) { again(true); return; }
    @REF@ r = pr.getReference();
    if (r == null) { again(true); return; }
    @ST@ st = r.getStore();
    if (st == null) { again(true); return; }
    @PLA@ player = (@PLA@) st.getComponent(r, @PLA@.getComponentType());
    if (player == null) { again(true); return; }
    if (st.getComponent(r, @DEATH@.getComponentType()) != null) { again(true); return; }
    Object cp = player.getPageManager().getCustomPage();
    if (cp instanceof @PKG@.ProfilePage) return;
    if (cp != null) { again(true); return; }
    boolean writable = true;
    try {
      com.hypixel.hytale.server.core.io.PacketHandler ph = player.getPlayerConnection();
      com.hypixel.hytale.protocol.io.ChannelConnection ch = ph == null ? null : ph.getChannel(com.hypixel.hytale.protocol.NetworkChannel.WorldMap);
      writable = ch == null || ch.isWritable();
    } catch (Throwable t) { writable = true; }
    if (writable) this.calm++; else this.calm = 0;
    if (this.calm < 2) { again(false); return; }
    player.getPageManager().openCustomPage(r, st, new @PKG@.ProfilePage(pr, 1, true));
    @PKG@.ProfStore.markPrompted(u);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not open the Create Profile page on join: " + t); }
}""")

# ================= RecoverTask: apply a crash-recovery plan on the player's world thread (started at PlayerConnectEvent; polls until the
# player entity is in a world - it does NOT wait for PlayerReadyEvent, so other mods get as little time as possible with a live inventory
# that belongs to another profile; profile:busy:<uuid> is set the whole time) =================
rec.addInterface(pool.get("java.lang.Runnable"))
F(rec, "public @PR@ pr;")
F(rec, "public org.bson.BsonDocument doc;")
F(rec, "public String which;")
F(rec, "public String how;")
F(rec, "public boolean onWorld;")
F(rec, "public int tries;")
F(rec, "public java.util.UUID hopWorld;")
C(rec, r"""
public RecoverTask(@PR@ pr, org.bson.BsonDocument doc, String which, String how) {
  this.pr = pr; this.doc = doc; this.which = which; this.how = how; this.onWorld = false; this.tries = 0;
}""")
M(rec, r"""
public void later(long ms) {
  this.onWorld = false;
  @HSV@.SCHEDULED_EXECUTOR.schedule(this, ms, java.util.concurrent.TimeUnit.MILLISECONDS);
}""")
M(rec, r"""
public void giveUp() {
  if (pr != null) { @PKG@.ProfStore.busy(pr.getUuid(), false); @PKG@.ProfStore.BUSY.remove(pr.getUuid()); }
  @PKG@.ProfCfg.warn("crash recovery postponed for " + (pr == null ? "?" : String.valueOf(pr.getUuid())) + " - the marker stays (retried at the first PlayerReadyEvent, else the next login)");
}""")
M(rec, r"""
public void run() {
  try {
    if (pr == null || !pr.isValid()) { giveUp(); return; }
    if (!this.onWorld) {
      java.util.UUID wu = pr.getWorldUuid();
      @WLD@ w = wu == null ? null : @UNI@.get().getWorld(wu);
      if (w == null) { if (++this.tries < 240) later(250L); else giveUp(); return; }
      this.onWorld = true;
      this.hopWorld = wu;
      w.execute(this);
      return;
    }
    java.util.UUID nowWorld = pr.getWorldUuid();
    if (nowWorld == null || !nowWorld.equals(this.hopWorld)) { if (++this.tries < 240) later(250L); else giveUp(); return; }
    @REF@ r = pr.getReference();
    @ST@ st = r == null ? null : r.getStore();
    @PLA@ player = null;
    if (st != null) player = (@PLA@) st.getComponent(r, @PLA@.getComponentType());
    if (player == null) { if (++this.tries < 240) later(250L); else giveUp(); return; }
    @PKG@.ProfSwitch.recoverApply(st, r, pr, player, this.doc, this.which, this.how);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("crash recovery failed for " + (pr == null ? "?" : String.valueOf(pr.getUuid())) + ": " + t); }
  if (pr != null) { @PKG@.ProfStore.busy(pr.getUuid(), false); @PKG@.ProfStore.BUSY.remove(pr.getUuid()); }
}""")

# ================= ProfJoin: the join work - PlayerConnectEvent (engine join thread, before the player is in any world) =================
# plan = recoverPlan result: { snapshot, profile id, "rolled back"|"finished" } -> RecoverTask; { null, null, "failed" } -> chat note
M(pj, r"""
public static void startRecover(@PR@ pr, Object[] plan) {
  if (plan == null) return;
  java.util.UUID u = pr.getUuid();
  if (plan[0] == null) {
    @PKG@.ProfStore.notice(pr, @MSG@.raw("[Profiles] A profile switch of yours failed earlier and needs an admin - your items are saved on the server. Profile switching is paused until then. Please tell an admin.").color("#ff9d6b"));
    return;
  }
  if (@PKG@.ProfStore.BUSY.putIfAbsent(u, Boolean.TRUE) != null) return;
  @PKG@.ProfStore.busy(u, true);
  new @PKG@.RecoverTask(pr, (org.bson.BsonDocument) plan[0], (String) plan[1], (String) plan[2]).later(0L);
}""")
# order of bridge writes: (profile:busy:<uuid> when a recovery is pending) -> key, id, class, name, list -> epoch (publish)
M(pj, r"""
public static void prepare(@PR@ pr) {
  java.util.UUID u = pr.getUuid();
  @PKG@.ProfStore.reload(u);
  if (@PKG@.ProfStore.BROKEN.containsKey(u)) { @PKG@.ProfStore.publish(u); return; }
  @PKG@.ProfStore.noteLogin(u, pr.getUsername());
  java.util.Properties p = @PKG@.ProfStore.load(u);
  if (@PKG@.ProfStore.activeOf(p) == null && @PKG@.ProfStore.count(p) > 0) {
    String low = @PKG@.ProfStore.lowestId(p);
    @PKG@.ProfCfg.warn("player " + u + " has profiles but no valid active one - activating profile " + low);
    @PKG@.ProfStore.setActive(u, null, low);
  }
  if (!@PKG@.ProfStore.BUSY.containsKey(u)) startRecover(pr, @PKG@.ProfSwitch.recoverPlan(u));
  @PKG@.ProfStore.publish(u);
}""")
# first PlayerReadyEvent: a stage=saved marker nobody is working on (a connect-time recovery that gave up) gets another try
M(pj, r"""
public static void retryRecover(@PR@ pr) {
  java.util.UUID u = pr.getUuid();
  if (@PKG@.ProfStore.BUSY.containsKey(u) || @PKG@.ProfStore.PENDING.containsKey(u)) return;
  java.util.Properties m = @PKG@.ProfSwitch.readMarker(u);
  if (m == null || !"saved".equals(m.getProperty("stage", ""))) return;
  startRecover(pr, @PKG@.ProfSwitch.recoverPlan(u));
}""")

# ================= ReadyTask: first ready of the session, OFF the world thread (messages; join work only as a fallback) =================
# 0.1.1: the "Playing profile" line is the one player-switchable message (profiles.loginStatus, Settings-Spec 3.9). Only the send is gated;
# the join work, notices, the broken-file warning and the welcome / Create page (setup the player must finish, Settings-Spec 2.3) are not.
# ReadyTask runs on the scheduler, so a first settings-file read inside settings:fn:get is fine here.
rtk.addInterface(pool.get("java.lang.Runnable"))
F(rtk, "public @PR@ pr;")
C(rtk, "public ReadyTask(@PR@ pr) { this.pr = pr; }")
M(rtk, r"""
public void run() {
  try {
    if (pr == null || !pr.isValid()) return;
    java.util.UUID u = pr.getUuid();
    if (@PKG@.ProfStore.JOINED.remove(u) == null) @PKG@.ProfJoin.prepare(pr);
    else @PKG@.ProfJoin.retryRecover(pr);
    Object note = @PKG@.ProfStore.NOTICE.remove(u);
    if (note != null) pr.sendMessage((@MSG@) note);
    java.util.Properties p = @PKG@.ProfStore.load(u);
    if (@PKG@.ProfStore.BROKEN.containsKey(u)) {
      pr.sendMessage(@MSG@.raw("[Profiles] Your profile file could not be read - profile changes are blocked. Please tell an admin.").color("#ff9d6b"));
      return;
    }
    String act = @PKG@.ProfStore.activeOf(p);
    if (act == null) {
      pr.sendMessage(@MSG@.raw("[Profiles] Welcome! Create your profile: you pick your class (" + @PKG@.ProfRoster.choiceText() + ") and it is locked to that profile. /profiles").color("#ffc800"));
      if (@PKG@.ProfCfg.PROMPT_EVERY_LOGIN || !"1".equals(p.getProperty("prompted"))) new @PKG@.OpenTask(pr).later(@PKG@.ProfCfg.OPEN_DELAY_MS);
      return;
    }
    String cls = p.getProperty("p." + act + ".class", "");
    if (@PKG@.ProfCfg.notifyOn(u, "profiles.loginStatus"))
      pr.sendMessage(@MSG@.raw("[Profiles] Playing profile " + p.getProperty("p." + act + ".name", act) + (cls.length() > 0 ? " (" + cls + ")" : "") + ". /profiles to switch or create one.").color("#9fd8a2"));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("ready task failed: " + t); }
}""")
svt.addInterface(pool.get("java.lang.Runnable"))
F(svt, "public java.util.UUID u;")
C(svt, "public SaveTask(java.util.UUID u) { this.u = u; }")
M(svt, r"""
public void run() {
  try { if (u != null) @PKG@.ProfStore.touch(u); } catch (Throwable t) { }
}""")

# ================= PlayerReadyEvent (fires on EVERY world switch -> once per session) / disconnect =================
rdy.addInterface(pool.get("java.util.function.Consumer"))
C(rdy, "public ProfReady() { }")
M(rdy, r"""
public void accept(Object ev) {
  try {
    @PRE@ e = (@PRE@) ev;
    @REF@ r = e.getPlayerRef();
    if (r == null) return;
    @ST@ st = r.getStore();
    if (st == null) return;
    @PR@ pr = (@PR@) st.getComponent(r, @PR@.getComponentType());
    if (pr == null) return;
    java.util.UUID u = pr.getUuid();
    if (@PKG@.ProfStore.SESSION.putIfAbsent(u, Boolean.TRUE) != null) return;
    @HSV@.SCHEDULED_EXECUTOR.execute(new @PKG@.ReadyTask(pr));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("ready handler failed: " + t); }
}""")
# PlayerConnectEvent: fired by Universe.addPlayer after the engine loaded the player's data, on the engine's join thread (a storage /
# network thread, never a world thread) and BEFORE the player is added to any world - registerGlobal, as BetterMap and Essentials do.
pcon.addInterface(pool.get("java.util.function.Consumer"))
C(pcon, "public ProfConnect() { }")
M(pcon, r"""
public void accept(Object ev) {
  try {
    @PR@ pr = ((@PCE@) ev).getPlayerRef();
    if (pr == null) return;
    @PKG@.ProfJoin.prepare(pr);
    @PKG@.ProfStore.JOINED.put(pr.getUuid(), Boolean.TRUE);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("connect handler failed (the first PlayerReadyEvent retries): " + t); }
}""")
quit_.addInterface(pool.get("java.util.function.Consumer"))
C(quit_, "public ProfQuit() { }")
M(quit_, r"""
public void accept(Object ev) {
  try {
    @PR@ pr = ((@PDE@) ev).getPlayerRef();
    if (pr == null) return;
    java.util.UUID u = pr.getUuid();
    @PKG@.ProfStore.SESSION.remove(u);
    @PKG@.ProfStore.JOINED.remove(u);
    @HSV@.SCHEDULED_EXECUTOR.execute(new @PKG@.SaveTask(u));
  } catch (Throwable t) { }
}""")

# ================= /profiles create | switch <n> | list, /profiles (alias /profile) =================
C(pcre, r"""
public ProfCreateCmd() {
  super("create", "Create a new profile - pick its class, locked to that profile forever");
  addAliases(new String[] { "new" });
  setPermissionGroups(new String[] { "hytale:Adventurer" });
}""")
M(pcre, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  try {
    String err = @PKG@.ProfilePage.open(store, ref, pr, 1);
    if (err != null) pr.sendMessage(@MSG@.raw("[Profiles] " + err));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("/profiles create failed: " + t); pr.sendMessage(@MSG@.raw("[Profiles] Could not open the page.")); }
}""")
F(psw, "public @RA@ profArg;")
C(psw, r"""
public ProfSwitchCmd() {
  super("switch", "Switch to another of your profiles: /profiles switch <number or name>");
  this.profArg = withRequiredArg("profile", "profile number (1, 2, ...) or name", @ATY@.STRING);
  setPermissionGroups(new String[] { "hytale:Adventurer" });
}""")
M(psw, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  try {
    java.util.UUID u = pr.getUuid();
    String arg = String.valueOf(ctx.get(this.profArg));
    String id = @PKG@.ProfStore.resolveId(u, arg);
    if (id == null) { pr.sendMessage(@MSG@.raw("[Profiles] No profile '" + arg + "'. Yours: " + @PKG@.ProfStore.describe(u))); return; }
    @PLA@ player = (@PLA@) store.getComponent(ref, @PLA@.getComponentType());
    if (player == null) return;
    String err = @PKG@.ProfSwitch.switchTo(store, ref, pr, player, id);
    if (err != null) pr.sendMessage(@MSG@.raw("[Profiles] " + err).color("#ff9d6b"));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("/profiles switch failed: " + t); pr.sendMessage(@MSG@.raw("[Profiles] Usage: /profiles switch <number or name>")); }
}""")
C(plst, r"""
public ProfListCmd() {
  super("list", "List your profiles in chat");
  setPermissionGroups(new String[] { "hytale:Adventurer" });
}""")
M(plst, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  pr.sendMessage(@MSG@.raw("[Profiles] " + @PKG@.ProfStore.describe(pr.getUuid())));
}""")
C(pcmd, r"""
public ProfilesCmd() {
  super("profiles", "Your profiles (each = its own class, island and inventory): /profiles | create | switch <n> | list");
  addAliases(new String[] { "profile" });
  setPermissionGroups(new String[] { "hytale:Adventurer" });
  addSubCommand(new @PKG@.ProfCreateCmd());
  addSubCommand(new @PKG@.ProfSwitchCmd());
  addSubCommand(new @PKG@.ProfListCmd());
}""")
M(pcmd, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  try {
    String err = @PKG@.ProfilePage.open(store, ref, pr, 0);
    if (err != null) pr.sendMessage(@MSG@.raw("[Profiles] " + err));
  } catch (Throwable t) {
    @PKG@.ProfCfg.warn("/profiles failed: " + t);
    pr.sendMessage(@MSG@.raw("[Profiles] Could not open the page. Your profiles: " + @PKG@.ProfStore.describe(pr.getUuid())));
  }
}""")

# ================= /profileadmin info | setclass | reload (perm skyyprofiles.admin) =================
F(ainf, "public @RA@ playerArg;")
C(ainf, r"""
public AdmInfoCmd() {
  super("info", "(admin) A player's profiles: /profileadmin info <player|uuid>");
  requirePermission("skyyprofiles.admin");
  this.playerArg = withRequiredArg("player", "player name (online, or seen before) or uuid", @ATY@.STRING);
}""")
M(ainf, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  try {
    if (!pr.hasPermission("skyyprofiles.admin")) { pr.sendMessage(@MSG@.raw("[Profiles] no permission (skyyprofiles.admin)")); return; }
    java.util.UUID t = @PKG@.ProfStore.resolve(String.valueOf(ctx.get(this.playerArg)));
    if (t == null) { pr.sendMessage(@MSG@.raw("[Profiles] Unknown player - use a name (online or seen before) or a uuid.")); return; }
    java.util.Properties p = @PKG@.ProfStore.load(t);
    String act = @PKG@.ProfStore.activeOf(p);
    pr.sendMessage(@MSG@.raw("[Profiles] " + p.getProperty("username", "?") + " (" + t + "): " + @PKG@.ProfStore.describe(t)));
    pr.sendMessage(@MSG@.raw("[Profiles] active key " + @PKG@.ProfStore.keyFor(t, act) + " | epoch " + @PKG@.ProfStore.num(p, "epoch") + " | switches " + @PKG@.ProfStore.num(p, "switches")
      + (@PKG@.ProfStore.BROKEN.containsKey(t) ? " | FILE UNREADABLE" : "") + (@PKG@.ProfSwitch.readMarker(t) != null ? " | switch marker present (switching/" + t + ".properties)" : "")));
  } catch (Throwable x) { pr.sendMessage(@MSG@.raw("[Profiles] Usage: /profileadmin info <player|uuid>")); }
}""")
F(acls, "public @RA@ playerArg;")
F(acls, "public @RA@ profArg;")
F(acls, "public @RA@ classArg;")
C(acls, r"""
public AdmSetClassCmd() {
  super("setclass", "(admin) Fix the class of a profile: /profileadmin setclass <player|uuid> <profile number> <class>");
  requirePermission("skyyprofiles.admin");
  this.playerArg = withRequiredArg("player", "player name (online, or seen before) or uuid", @ATY@.STRING);
  this.profArg = withRequiredArg("profile", "profile number (1, 2, ...) or name", @ATY@.STRING);
  this.classArg = withRequiredArg("class", "archer | warrior | mage | berserker | priest (assassin, shaman later)", @ATY@.STRING);
}""")
M(acls, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  try {
    if (!pr.hasPermission("skyyprofiles.admin")) { pr.sendMessage(@MSG@.raw("[Profiles] no permission (skyyprofiles.admin)")); return; }
    java.util.UUID t = @PKG@.ProfStore.resolve(String.valueOf(ctx.get(this.playerArg)));
    if (t == null) { pr.sendMessage(@MSG@.raw("[Profiles] Unknown player - use a name (online or seen before) or a uuid.")); return; }
    String id = @PKG@.ProfStore.resolveId(t, String.valueOf(ctx.get(this.profArg)));
    if (id == null) { pr.sendMessage(@MSG@.raw("[Profiles] That player has no such profile: " + @PKG@.ProfStore.describe(t))); return; }
    String[] c = @PKG@.ProfRoster.entry(String.valueOf(ctx.get(this.classArg)));
    if (c == null) { pr.sendMessage(@MSG@.raw("[Profiles] Unknown class. Classes: " + @PKG@.ProfRoster.choiceText())); return; }
    if (!@PKG@.ProfStore.setClass(t, id, c[0])) { pr.sendMessage(@MSG@.raw("[Profiles] Could not save the change (see the server log).")); return; }
    @PKG@.ProfStore.publish(t);
    @PKG@.ProfStore.log("SETCLASS " + t + " profile " + id + " -> " + c[0] + " by " + pr.getUsername());
    pr.sendMessage(@MSG@.raw("[Profiles] Profile " + id + " of " + t + " is now " + @PKG@.ProfRoster.article(c[0]) + ("1".equals(c[6]) ? "" : " (not selectable yet in SkyyClasses)") + ". " + @PKG@.ProfStore.describe(t)));
    @PR@ tp = @UNI@.get().getPlayer(t);
    if (tp != null && tp.isValid()) tp.sendMessage(@MSG@.raw("[Profiles] An admin set the class of your profile " + id + " to " + c[0] + ".").color("#ffc800"));
    // 0.1.2: a fix is not a pick - no class kit (spec 4.3 / Q14); tell the admin how to hand one out
    if (@PKG@.ProfKit.available()) pr.sendMessage(@MSG@.raw("[Profiles] No class kit is given for a class fix - /classadmin kit <player> [class] hands one out."));
  } catch (Throwable x) { pr.sendMessage(@MSG@.raw("[Profiles] Usage: /profileadmin setclass <player|uuid> <profile number> <class>")); }
}""")
# 0.1.1 - config helpers for the admin commands (the kit classes are compiled above). config.properties is re-read by the kit's reload
# op, which logs hand edits (via=file) and runs ProfCfg.load AFTER its own pending writes - never read it here directly: a value set a
# moment ago in the menu may still be waiting for the kit's 500 ms save.
M(cfg, r"""
public static String cfgReload(java.util.UUID who, String name) {
  if (!@PKG@.CfgPub.STARTED) return "read directly (the config kit did not start - see the server log): " + load();
  try {
    Object o = new @PKG@.CfgFn().apply(new Object[] { "reload", who, name, "command" });
    if (o instanceof Object[] && ((Object[]) o).length > 2) return String.valueOf(((Object[]) o)[2]);
  } catch (Throwable t) { warn("config reload failed: " + t); }
  return "not re-read - see the server log";
}""")
M(cfg, r"""
public static String rowKey(String typed) {
  if (typed == null) return null;
  String s = typed.trim();
  for (int i = 0; i < @PKG@.CfgRows.KEYS.length; i++) if (@PKG@.CfgRows.KEYS[i].equalsIgnoreCase(s)) return @PKG@.CfgRows.KEYS[i];
  return null;
}""")
M(cfg, r"""
public static String keyList() {
  StringBuilder sb = new StringBuilder();
  for (int i = 0; i < @PKG@.CfgRows.KEYS.length; i++) { if (i > 0) sb.append(", "); sb.append(@PKG@.CfgRows.KEYS[i]); }
  return sb.toString();
}""")
C(arel, r"""
public AdmReloadCmd() {
  super("reload", "(admin) Re-read Skyy_SkyyProfiles/config.properties and the player files");
  requirePermission("skyyprofiles.admin");
}""")
M(arel, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  if (!pr.hasPermission("skyyprofiles.admin")) { pr.sendMessage(@MSG@.raw("[Profiles] no permission (skyyprofiles.admin)")); return; }
  String c = @PKG@.ProfCfg.cfgReload(pr.getUuid(), pr.getUsername());
  int re = @PKG@.ProfStore.reloadAll();
  int n = 0;
  try {
    java.util.Iterator it = @UNI@.get().getPlayers().iterator();
    while (it.hasNext()) { @PR@ p = (@PR@) it.next(); if (p != null) { @PKG@.ProfStore.publish(p.getUuid()); n++; } }
  } catch (Throwable t) { }
  pr.sendMessage(@MSG@.raw("[Profiles] reloaded (" + re + " cached player files re-read, " + n + " online players republished"
    + (@PKG@.ProfStore.BROKEN.isEmpty() ? "" : ", " + @PKG@.ProfStore.BROKEN.size() + " UNREADABLE - see the server log") + "). Config: " + c));
}""")
# 0.1.1: /profileadmin config - every setting with its running value (read only; the kit's current value, never a file read)
C(acfg, r"""
public AdmConfigCmd() {
  super("config", "(admin) List the SkyyProfiles settings and their values (change: /profileadmin set <setting> <value>)");
  requirePermission("skyyprofiles.admin");
}""")
M(acfg, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  if (!pr.hasPermission("skyyprofiles.admin")) { pr.sendMessage(@MSG@.raw("[Profiles] no permission (skyyprofiles.admin)")); return; }
  try {
    pr.sendMessage(@MSG@.raw("[Profiles] Settings (also in SkyWynn Menu > Server Setup > Profiles). Change: /profileadmin set <setting> <value|default>").color("#ffc800"));
    for (int i = 0; i < @PKG@.CfgRows.KEYS.length; i++) {
      String v = @PKG@.CfgFn.cmdGet(@PKG@.CfgRows.KEYS[i]);
      pr.sendMessage(@MSG@.raw("  " + @PKG@.CfgRows.KEYS[i] + " = " + @PKG@.CfgRows.disp(i, v) + "  - " + @PKG@.CfgRows.LABELS[i]
        + (@PKG@.CfgRows.flag(i, "new") ? " (new profiles only)" : "")));
    }
  } catch (Throwable t) { @PKG@.ProfCfg.warn("/profileadmin config failed: " + t); pr.sendMessage(@MSG@.raw("[Profiles] config: " + @PKG@.ProfCfg.summary())); }
}""")
# 0.1.1: /profileadmin set <setting> <value|default> - the menu's path (CfgFn.cmdSet): validated (typed values are refused, never
# clamped), confirm implied, written to config.properties line by line, versioned and logged via=command with the admin's name
F(aset, "public @RA@ keyArg;")
F(aset, "public @RA@ valArg;")
C(aset, r"""
public AdmSetCmd() {
  super("set", "(admin) Change a SkyyProfiles setting (logged): /profileadmin set <setting> <value|default>");
  requirePermission("skyyprofiles.admin");
  this.keyArg = withRequiredArg("setting", "maxProfiles, combatSeconds, keepItems, ... (/profileadmin config lists them)", @ATY@.STRING);
  this.valArg = withRequiredArg("value", "the new value (keepItems: a comma list without spaces, or none), or default", @ATY@.STRING);
}""")
M(aset, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  if (!pr.hasPermission("skyyprofiles.admin")) { pr.sendMessage(@MSG@.raw("[Profiles] no permission (skyyprofiles.admin)")); return; }
  try {
    String typed = String.valueOf(ctx.get(this.keyArg));
    String k = @PKG@.ProfCfg.rowKey(typed);
    if (k == null) { pr.sendMessage(@MSG@.raw("[Profiles] Unknown setting '" + typed + "'. Settings: " + @PKG@.ProfCfg.keyList())); return; }
    String v = String.valueOf(ctx.get(this.valArg));
    String low = v.trim().toLowerCase();
    boolean items = "items".equals(@PKG@.CfgRows.TYPES[@PKG@.CfgRows.index(k)]);
    if (low.equals("default")) v = null;
    else if (items && low.equals("none")) v = "";
    String msg = @PKG@.CfgFn.cmdSet(k, v, pr.getUuid(), pr.getUsername());
    if (v == null && items && msg != null && msg.startsWith("Unknown item: @DEFKEEP@"))
      msg = msg + " @DEFKEEP@ is SkyyMenu's item, so the default is only accepted while SkyyMenu is installed (without it that item does not"
        + " exist and keeping it does nothing). Keep nothing: /profileadmin set " + k + " none. Or put " + k + "=@DEFKEEP@ in"
        + " config.properties and run /profileadmin reload.";
    pr.sendMessage(@MSG@.raw("[Profiles] " + msg));
  } catch (Throwable t) { @PKG@.ProfCfg.warn("/profileadmin set failed: " + t); pr.sendMessage(@MSG@.raw("[Profiles] Usage: /profileadmin set <setting> <value|default> (keepItems: none = keep nothing)")); }
}""".replace("@DEFKEEP@", DEF_KEEP))
C(adm, r"""
public ProfileAdminCmd() {
  super("profileadmin", "(admin) /profileadmin info <player> | setclass <player> <n> <class> | config | set <setting> <value> | reload");
  requirePermission("skyyprofiles.admin");
  addSubCommand(new @PKG@.AdmInfoCmd());
  addSubCommand(new @PKG@.AdmSetClassCmd());
  addSubCommand(new @PKG@.AdmReloadCmd());
  addSubCommand(new @PKG@.AdmConfigCmd());
  addSubCommand(new @PKG@.AdmSetCmd());
}""")
M(adm, r"""
protected void execute(@CTX@ ctx, @ST@ store, @REF@ ref, @PR@ pr, @WLD@ world) {
  pr.sendMessage(@MSG@.raw("[Profiles] /profileadmin info <player|uuid> | setclass <player|uuid> <profile> <class> | config | set <setting> <value|default> | reload"));
  pr.sendMessage(@MSG@.raw("[Profiles] config: " + @PKG@.ProfCfg.summary()));
}""")

# ================= plugin =================
C(pl, "public SkyyProfilesPlugin(@JPI@ init) { super(init); }")
M(pl, r"""
public void setup() {
  @PKG@.ProfCfg.LOG = getLogger();
  java.nio.file.Path base = getDataDirectory().resolveSibling("Skyy_SkyyProfiles");
  @PKG@.ProfCfg.FILE = base.resolve("config.properties");
  @PKG@.ProfStore.DIR = base.resolve("players");
  @PKG@.ProfStore.SWDIR = base.resolve("switching");
  @PKG@.ProfStore.INVDIR = base.resolve("inventories");
  @PKG@.ProfStore.LOGF = base.resolve("switches.log");
  String up = @PKG@.ProfCfg.upgradeDefault();
  String cfgText = @PKG@.ProfCfg.load();
  if (up != null) @PKG@.ProfCfg.info(up);
  try {
    java.nio.file.Files.createDirectories(@PKG@.ProfStore.DIR, new java.nio.file.attribute.FileAttribute[0]);
    java.nio.file.Files.createDirectories(@PKG@.ProfStore.SWDIR, new java.nio.file.attribute.FileAttribute[0]);
    java.nio.file.Files.createDirectories(@PKG@.ProfStore.INVDIR, new java.nio.file.attribute.FileAttribute[0]);
  } catch (Throwable t) { @PKG@.ProfCfg.warn("could not create the data folders: " + t); }
  int markers = 0;
  try {
    java.nio.file.DirectoryStream ds = java.nio.file.Files.newDirectoryStream(@PKG@.ProfStore.SWDIR, "*.properties");
    try { java.util.Iterator it = ds.iterator(); while (it.hasNext()) { it.next(); markers++; } } finally { ds.close(); }
  } catch (Throwable t) { }
  @PKG@.ProfCfg.bridge().put("profile:fn:key", new @PKG@.KeyFn());
  getCommandRegistry().registerCommand(new @PKG@.ProfilesCmd());
  getCommandRegistry().registerCommand(new @PKG@.ProfileAdminCmd());
  getEventRegistry().registerGlobal(@PCE@.class, new @PKG@.ProfConnect());
  getEventRegistry().registerGlobal(@PRE@.class, new @PKG@.ProfReady());
  getEventRegistry().registerGlobal(@PDE@.class, new @PKG@.ProfQuit());
  getLogger().at(java.util.logging.Level.INFO).log("[SkyyProfiles] __VER__ ready - /profiles, /profileadmin; " + cfgText
    + (markers > 0 ? "; " + markers + " interrupted switch(es) will be finished or rolled back when those players join" : ""));
  // 0.1.1: player Settings switch (SkyyMenu 0.2+; load order does not matter: settings:def is the fallback) and, LAST, the admin config
  // kit: it reads config.properties after the load above and publishes config:def / config:fn / config:epoch:SkyyProfiles
  @PKG@.ProfCfg.regSetting("profiles.loginStatus", "Profile on login", "profiles", true, "Playing profile Strawberry (Archer) - when you join");
  @PKG@.CfgPub.start(getDataDirectory().getParent(), getLogger());
}""".replace("__VER__", VERSION))
# profile:fn:key stays registered on purpose (other mods' shutdown saves must still resolve the active profile). Graceful stop: the engine
# saves every online player, so the switch markers kept for the engine save (ClearLater) are no longer needed.
M(pl, r"""
protected void shutdown() {
  try {
    int n = @PKG@.ClearLater.clearAllNow();
    if (n > 0) @PKG@.ProfCfg.info("shutdown: cleared " + n + " switch marker(s) that were waiting for the engine's player save");
  } catch (Throwable t) { }
  try { @PKG@.CfgPub.shutdown(); } catch (Throwable t) { }
  super.shutdown();
}""")

ALL = (cfg, ros, pkit, nam, sto, pub, kfn, inv, sw, clr, page, opn, rec, pj, rtk, svt, rdy, pcon, quit_, pcre, psw, plst, pcmd, ainf, acls, arel,
       acfg, aset, adm, pl)
for c in ALL:
    c.writeFile(OUT)
kit.write(OUT)      # the kit's deferred checks (ProfCfg.load exists as the RELOAD routine), then its 7 classes
print("classes written:", len(ALL) + len(kit.classes), "(%d kit)" % len(kit.classes))

jar = os.path.join(HERE, "SkyyProfiles-%s.jar" % VERSION)
m = B.manifest("SkyyProfiles", VERSION, "SkyWynn profiles (SkyBlock style): each profile is its own save - class (Archer, Warrior, Mage, Berserker or Priest; picked at creation, locked, with its class kit from SkyyClasses), island, coins, skills, bags and vanilla inventory. /profiles. Zero dependencies; SkyyClasses and SkyyIslands are used when present.", PKG + ".SkyyProfilesPlugin")
m["IncludesAssetPack"] = False
B.assemble(jar, m, OUT)
if "--deploy" in sys.argv:
    raise SystemExit("SkyyProfiles: --deploy is not supported here - deploys go through tools/deploy_set.py")
